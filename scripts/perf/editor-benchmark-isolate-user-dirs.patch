diff --git a/src/core/types.rs b/src/core/types.rs
index 408d61f..edfb918 100644
--- a/src/core/types.rs
+++ b/src/core/types.rs
@@ -117,4 +117,13 @@ pub struct EditorConfig {
     /// Cold start timeout in seconds (per editor). Defaults to 10.
     #[serde(default)]
     pub cold_start_timeout_s: Option<u64>,
+    /// If true, every iteration runs with a freshly-empty per-iteration
+    /// `HOME` / `XDG_CACHE_HOME` / `XDG_CONFIG_HOME` / `XDG_DATA_HOME` /
+    /// `XDG_STATE_HOME`. Use this to measure true first-run startup, where
+    /// the editor must populate its caches (e.g. extracting embedded
+    /// plugins, generating type scaffolding) on every iteration. With this
+    /// off (the default), iteration #0 pays the first-run cost and #1+ run
+    /// against a warm cache, which biases the median toward the warm case.
+    #[serde(default)]
+    pub isolate_user_dirs: Option<bool>,
 }
diff --git a/src/runner/orchestrator.rs b/src/runner/orchestrator.rs
index 15be8df..f410bbc 100644
--- a/src/runner/orchestrator.rs
+++ b/src/runner/orchestrator.rs
@@ -223,7 +223,7 @@ impl Orchestrator {
             }
             ScenarioKind::SingleKeystroke => {
                 EditorProcess::kill_all(&editor.command)?;
-                let mut process = EditorProcess::spawn(editor)?;
+                let mut process = EditorProcess::spawn(editor, &[])?;
                 let rect =
                     self.find_editor_window(editor, process.pid, Duration::from_secs(30))?;
                 self.capturer.start_capture(rect)?;
@@ -253,7 +253,7 @@ impl Orchestrator {
             }
             ScenarioKind::ContinuousTyping { wpm, char_count } => {
                 EditorProcess::kill_all(&editor.command)?;
-                let mut process = EditorProcess::spawn(editor)?;
+                let mut process = EditorProcess::spawn(editor, &[])?;
                 let rect =
                     self.find_editor_window(editor, process.pid, Duration::from_secs(30))?;
                 self.capturer.start_capture(rect)?;
diff --git a/src/runner/process.rs b/src/runner/process.rs
index c91ac9b..c64c168 100644
--- a/src/runner/process.rs
+++ b/src/runner/process.rs
@@ -40,18 +40,31 @@ impl EditorProcess {
     /// Spawn an editor process according to its configuration.
     /// For GUI editors: spawns the editor directly.
     /// For Terminal editors: spawns the terminal emulator with the editor as an argument.
-    pub fn spawn(config: &EditorConfig) -> Result<Self, BenchmarkError> {
+    ///
+    /// `extra_env` is layered on top of the inherited environment. Use it
+    /// (e.g. with `XDG_CACHE_HOME`/`HOME`) to isolate per-iteration user
+    /// state when measuring first-run startup. For terminal editors the
+    /// env is set on the terminal emulator process; the shell and editor
+    /// inherit it.
+    pub fn spawn(
+        config: &EditorConfig,
+        extra_env: &[(String, String)],
+    ) -> Result<Self, BenchmarkError> {
         let mut gate_fifo = None;
         let child = match &config.kind {
-            EditorKind::Gui => Command::new(&config.command)
-                .args(&config.args)
-                .spawn()
-                .map_err(|e| {
+            EditorKind::Gui => {
+                let mut cmd = Command::new(&config.command);
+                cmd.args(&config.args);
+                for (k, v) in extra_env {
+                    cmd.env(k, v);
+                }
+                cmd.spawn().map_err(|e| {
                     BenchmarkError::Process(format!(
                         "Failed to spawn GUI editor '{}': {e}",
                         config.command
                     ))
-                })?,
+                })?
+            }
             EditorKind::Terminal => {
                 let terminal = config.terminal_emulator.as_deref().unwrap_or("alacritty");
                 // Use a fifo (named pipe) as a gate:
@@ -80,6 +93,9 @@ impl EditorProcess {
                 );
                 let mut cmd = Command::new(terminal);
                 cmd.arg("-e").arg("sh").arg("-c").arg(&shell_cmd);
+                for (k, v) in extra_env {
+                    cmd.env(k, v);
+                }
                 gate_fifo = Some(fifo_path);
 
                 cmd.spawn().map_err(|e| {
diff --git a/src/runner/scenarios.rs b/src/runner/scenarios.rs
index c99335f..573f9ad 100644
--- a/src/runner/scenarios.rs
+++ b/src/runner/scenarios.rs
@@ -257,9 +257,40 @@ pub fn run_cold_start(
             }
         }
 
+        // If the editor wants per-iteration user-dir isolation (true
+        // first-run startup, with empty caches), build a fresh
+        // HOME/XDG_* tree under iter_dir and pass it as extra env. The
+        // editor sees an empty `~/.cache`, `~/.config`, etc. on every
+        // iteration, so any first-run work (e.g. extracting embedded
+        // plugins to a content-addressed cache) is paid every time
+        // instead of only on iteration #0.
+        let extra_env: Vec<(String, String)> = if iter_config
+            .isolate_user_dirs
+            .unwrap_or(false)
+        {
+            let xdg_root = format!("{iter_dir}/_xdg");
+            for sub in ["home", "cache", "config", "data", "state"] {
+                let _ = std::fs::create_dir_all(format!("{xdg_root}/{sub}"));
+            }
+            debug!(
+                xdg_root,
+                editor = %editor_config.name,
+                "isolating user dirs for this iteration"
+            );
+            vec![
+                ("HOME".to_string(), format!("{xdg_root}/home")),
+                ("XDG_CACHE_HOME".to_string(), format!("{xdg_root}/cache")),
+                ("XDG_CONFIG_HOME".to_string(), format!("{xdg_root}/config")),
+                ("XDG_DATA_HOME".to_string(), format!("{xdg_root}/data")),
+                ("XDG_STATE_HOME".to_string(), format!("{xdg_root}/state")),
+            ]
+        } else {
+            Vec::new()
+        };
+
         let start = Instant::now();
 
-        let mut process = EditorProcess::spawn(&iter_config)?;
+        let mut process = EditorProcess::spawn(&iter_config, &extra_env)?;
         info!(pid = process.pid, editor = %editor_config.name, "spawned editor");
 
         let rect = window_finder.find_and_focus(
