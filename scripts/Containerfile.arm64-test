FROM ubuntu:20.04

RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

RUN useradd -m -s /bin/bash testuser

# Start as root by default. Use "su - testuser" to switch.
WORKDIR /work
