# Screenshots

Evidence images referenced by internal docs and by the pull requests that
changed the surfaces they show. Captured headlessly (Playwright + Chromium
against `fresh --web`, or the homepage served locally), downscaled and
palette-quantised so the repository stays small.

Named `<issue>-<subject>[-before|-after].png`.
