{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Config",
  "description": "Main configuration structure",
  "type": "object",
  "properties": {
    "version": {
      "description": "Config format version, used for migration. Missing means version 0.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0,
      "default": 0
    },
    "theme": {
      "description": "Color theme name",
      "$ref": "#/$defs/ThemeOptions",
      "default": "high-contrast"
    },
    "locale": {
      "description": "UI language. If not set, detected from LC_ALL, LC_MESSAGES or LANG.",
      "$ref": "#/$defs/LocaleOptions",
      "default": null
    },
    "check_for_updates": {
      "description": "Check for new versions on startup (default: true).\nAlso sends basic anonymous telemetry (version, OS, terminal type).",
      "type": "boolean",
      "default": true
    },
    "self_update": {
      "description": "Offer to update from inside the editor when a new version is found (default: true).\nWhen off, the status-bar indicator only tells you an update exists.\nNeeds `check_for_updates` and an install method that can self-update.",
      "type": "boolean",
      "default": true
    },
    "orchestrator_mode": {
      "description": "When on (default), `fresh` with no arguments reattaches to a single background\neditor with the workspace dock and your last workspace. When off, it opens a\nplain editor in the current directory; runs with files or flags are unaffected.",
      "type": "boolean",
      "default": true,
      "x-order": 1
    },
    "editor": {
      "description": "Editor behavior settings (indentation, line numbers, wrapping, etc.)",
      "$ref": "#/$defs/EditorConfig",
      "default": {
        "animations": true,
        "cursor_jump_animation": true,
        "viewport_edge_fade": true,
        "line_numbers": true,
        "relative_line_numbers": false,
        "highlight_current_line": true,
        "highlight_occurrences": true,
        "hide_current_line_on_selection": false,
        "highlight_current_column": false,
        "line_wrap": true,
        "wrap_indent": true,
        "wrap_column": null,
        "page_width": 80,
        "syntax_highlighting": true,
        "show_menu_bar": true,
        "screensaver_enabled": false,
        "screensaver_idle_minutes": 5,
        "menu_bar_mnemonics": true,
        "show_tab_bar": true,
        "show_status_bar": true,
        "status_bar": {
          "left": [
            "{trust}",
            "{remote}",
            "{terminal_restart}",
            "{cursor}",
            "{diagnostics}",
            "{cursor_count}",
            "{messages}"
          ],
          "right": [
            "{read_only}",
            "{line_ending}",
            "{encoding}",
            "{language}",
            "{lsp}",
            "{warnings}",
            "{update}",
            "{palette}"
          ],
          "separator": ""
        },
        "search": {
          "case_sensitive": false,
          "whole_word": false,
          "regex": false,
          "confirm_each": false
        },
        "show_prompt_line": false,
        "show_vertical_scrollbar": true,
        "show_horizontal_scrollbar": false,
        "show_tilde": true,
        "nerd_font_icons": false,
        "use_terminal_bg": false,
        "set_window_title": true,
        "terminal_auto_title": true,
        "cursor_style": "default",
        "rulers": [],
        "indentation_guide": "none",
        "indentation_guide_glyph": "▏",
        "rainbow_indentation": false,
        "whitespace_show": true,
        "whitespace_spaces_leading": false,
        "whitespace_spaces_inner": false,
        "whitespace_spaces_trailing": false,
        "whitespace_tabs_leading": true,
        "whitespace_tabs_inner": true,
        "whitespace_tabs_trailing": true,
        "whitespace_newlines": false,
        "whitespace_carriage_returns": false,
        "whitespace_in_selection": true,
        "use_tabs": false,
        "tab_size": 4,
        "auto_indent": true,
        "auto_close": true,
        "auto_surround": true,
        "virtual_space": "off",
        "scroll_offset": 3,
        "default_line_ending": "lf",
        "trim_trailing_whitespace_on_save": false,
        "ensure_final_newline_on_save": false,
        "auto_read_only": true,
        "highlight_matching_brackets": true,
        "rainbow_brackets": true,
        "completion_popup_auto_show": false,
        "quick_suggestions": true,
        "quick_suggestions_delay_ms": 150,
        "suggest_on_trigger_characters": true,
        "enable_inlay_hints": true,
        "enable_semantic_tokens_full": false,
        "diagnostics_inline_text": false,
        "mouse_wheel_scroll_lines": 3,
        "smooth_scroll": true,
        "mouse_hover_enabled": true,
        "mouse_hover_delay_ms": 500,
        "double_click_time_ms": 500,
        "auto_save_enabled": false,
        "auto_save_interval_secs": 30,
        "hot_exit": true,
        "confirm_quit": false,
        "restore_previous_session": true,
        "skip_session_restore_when_files_passed": true,
        "ephemeral_file_patterns": [
          "**/.git/**"
        ],
        "auto_create_empty_buffer_on_last_buffer_close": true,
        "recovery_enabled": true,
        "auto_recovery_save_interval_secs": 2,
        "auto_revert_poll_interval_ms": 2000,
        "keyboard_disambiguate_escape_codes": true,
        "keyboard_report_event_types": false,
        "keyboard_report_alternate_keys": true,
        "keyboard_report_all_keys_as_escape_codes": false,
        "snapshot_interval": 100,
        "highlight_context_bytes": 10000,
        "large_file_threshold_bytes": 10485760,
        "estimated_line_length": 80,
        "read_concurrency": 64,
        "file_tree_poll_interval_ms": 3000
      }
    },
    "file_explorer": {
      "description": "File explorer panel settings",
      "$ref": "#/$defs/FileExplorerConfig",
      "default": {
        "respect_gitignore": true,
        "show_hidden": false,
        "show_gitignored": false,
        "custom_ignore_patterns": [],
        "width": "30%",
        "preview_tabs": true,
        "side": "left",
        "follow_active_buffer": false,
        "auto_open_on_last_buffer_close": true,
        "compact_directories": true,
        "tree_indicator_collapsed": ">",
        "tree_indicator_expanded": "▼"
      }
    },
    "sidebar": {
      "description": "Sidebar settings (the column that holds the file explorer).",
      "$ref": "#/$defs/SidebarConfig",
      "default": {
        "accordion": "free"
      }
    },
    "file_browser": {
      "description": "File browser settings (Open File dialog)",
      "$ref": "#/$defs/FileBrowserConfig",
      "default": {
        "show_hidden": false
      }
    },
    "clipboard": {
      "description": "Clipboard settings (which clipboard methods to use)",
      "$ref": "#/$defs/ClipboardConfig",
      "default": {
        "use_osc52": true,
        "use_system_clipboard": true
      }
    },
    "terminal": {
      "description": "Terminal settings",
      "$ref": "#/$defs/TerminalConfig",
      "default": {
        "jump_to_end_on_output": true,
        "shell": null,
        "skip_app_execution_alias": true,
        "resume_agents": true,
        "mouse_drag_selects": true,
        "mouse_forwarding": "requested"
      }
    },
    "keybindings": {
      "description": "Custom keybindings (overrides for the active map)",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Keybinding"
      },
      "default": [],
      "x-category": "Keybindings",
      "x-order": 2
    },
    "keybinding_maps": {
      "description": "Custom named keybinding maps. A map can inherit from another map.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/KeymapConfig"
      },
      "default": {},
      "x-category": "Keybindings",
      "x-order": 3
    },
    "active_keybinding_map": {
      "description": "Active keybinding map name",
      "$ref": "#/$defs/KeybindingMapOptions",
      "default": "default",
      "x-category": "Keybindings",
      "x-order": 1
    },
    "languages": {
      "description": "Per-language configuration overrides (tab size, formatters, etc.)",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/LanguageConfig"
      },
      "default": {},
      "x-category": "Syntax & Languages",
      "x-section": "Languages",
      "x-order": 1
    },
    "default_language": {
      "description": "Language used for files whose type can't be detected.\nMust be a key in `languages` (e.g. \"bash\"); its full settings apply.",
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "x-enum-from": "/languages",
      "x-category": "Syntax & Languages",
      "x-section": "Languages",
      "x-order": 2
    },
    "lsp_enabled": {
      "description": "Master switch for language servers. When off, no server starts automatically;\nyou can still start one with \"Start/Restart LSP Server\".",
      "type": "boolean",
      "default": true,
      "x-category": "Syntax & Languages",
      "x-section": "Language Servers",
      "x-order": 3
    },
    "lsp": {
      "description": "Language servers per language. Each language takes one server or a list.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/LspLanguageConfig"
      },
      "default": {},
      "x-category": "Syntax & Languages",
      "x-section": "Language Servers",
      "x-order": 4
    },
    "universal_lsp": {
      "description": "Language servers for all languages, run alongside those in `lsp`.\nKeyed by a unique server name (e.g. \"quicklsp\").",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/LspLanguageConfig"
      },
      "default": {},
      "x-category": "Syntax & Languages",
      "x-section": "Language Servers",
      "x-order": 5
    },
    "warnings": {
      "description": "Warning notification settings",
      "$ref": "#/$defs/WarningsConfig",
      "default": {
        "show_status_indicator": true
      }
    },
    "plugins": {
      "description": "Per-plugin settings, by plugin name. Use it to turn plugins on or off.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/PluginConfig"
      },
      "default": {},
      "x-standalone-category": true,
      "x-no-add": true
    },
    "packages": {
      "description": "Package manager settings for plugin/theme installation",
      "$ref": "#/$defs/PackagesConfig",
      "default": {
        "sources": [
          "https://github.com/sinelaw/fresh-plugins-registry"
        ]
      }
    },
    "env": {
      "description": "Auto-activation of project environments (venv, direnv, mise, …).",
      "$ref": "#/$defs/EnvConfig",
      "default": {
        "detectors": [
          {
            "name": ".venv",
            "markers": [
              ".venv"
            ],
            "kind": "path-only",
            "snippet": "source .venv/bin/activate",
            "require": [
              ".venv/bin/python",
              ".venv/bin/python3",
              ".venv/Scripts/python.exe"
            ]
          },
          {
            "name": "venv",
            "markers": [
              "venv"
            ],
            "kind": "path-only",
            "snippet": "source venv/bin/activate",
            "require": [
              "venv/bin/python",
              "venv/bin/python3",
              "venv/Scripts/python.exe"
            ]
          },
          {
            "name": "direnv",
            "markers": [
              ".envrc"
            ],
            "kind": "shell",
            "snippet": "eval \"$(direnv export bash)\"",
            "require": []
          },
          {
            "name": "mise",
            "markers": [
              "mise.toml",
              ".mise.toml",
              ".tool-versions"
            ],
            "kind": "shell",
            "snippet": "eval \"$(mise env -s bash)\"",
            "require": []
          },
          {
            "name": "pipenv",
            "markers": [
              "Pipfile"
            ],
            "kind": "shell",
            "snippet": "source \"$(pipenv --venv)/bin/activate\"",
            "require": []
          },
          {
            "name": "poetry",
            "markers": [
              "poetry.lock"
            ],
            "kind": "shell",
            "snippet": "source \"$(poetry env info --path)/bin/activate\"",
            "require": []
          }
        ]
      }
    }
  },
  "$defs": {
    "ThemeOptions": {
      "description": "Available color themes",
      "type": "string",
      "x-enum-from": "$themes"
    },
    "LocaleOptions": {
      "description": "UI locale (language). Use null for auto-detection from environment.",
      "enum": [
        null,
        "bg",
        "cs",
        "de",
        "en",
        "es",
        "fr",
        "it",
        "ja",
        "ko",
        "pt-BR",
        "ru",
        "th",
        "uk",
        "vi",
        "zh-CN"
      ]
    },
    "EditorConfig": {
      "description": "Editor behavior configuration",
      "type": "object",
      "properties": {
        "animations": {
          "description": "Enable UI animations (tab-switch slides, dashboard, plugin effects).\nTurn off for a fully static UI, e.g. on slow terminals or over SSH.",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "cursor_jump_animation": {
          "description": "Show a trail animation when the cursor jumps far (search, go-to-definition,\npane switch). Needs `animations` on.",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "viewport_edge_fade": {
          "description": "Fade out the top and bottom two rows of each pane when there is more\ntext beyond that edge. The edge the cursor is on is never faded.",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "line_numbers": {
          "description": "Show line numbers in the gutter (default for new buffers).",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "relative_line_numbers": {
          "description": "Show line numbers relative to the cursor line.",
          "type": "boolean",
          "default": false,
          "x-section": "Display"
        },
        "highlight_current_line": {
          "description": "Highlight the line containing the cursor",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "highlight_occurrences": {
          "description": "Highlight all occurrences of the word under the cursor",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "hide_current_line_on_selection": {
          "description": "Hide the current-line highlight while text is selected. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Display"
        },
        "highlight_current_column": {
          "description": "Highlight the column containing the cursor",
          "type": "boolean",
          "default": false,
          "x-section": "Display"
        },
        "line_wrap": {
          "description": "Wrap long lines to fit the window width (default for new views)",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "wrap_indent": {
          "description": "Indent wrapped lines to match the original line's indentation.",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "wrap_column": {
          "description": "Number of text columns to wrap lines at (e.g. `80`), not counting the\nline-number gutter. Never wider than the window.\n`null` or `0` (default) wraps at the window edge.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null,
          "x-section": "Display"
        },
        "page_width": {
          "description": "Text width in columns in page view, centered with margins. Default: 80.\n`null` or `0` uses the full window width.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": 80,
          "x-section": "Display"
        },
        "syntax_highlighting": {
          "description": "Enable syntax highlighting for code files.",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "show_menu_bar": {
          "description": "Show the menu bar (File, Edit, View, …) at the top. Can be toggled at any time.\nDefault: true",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "screensaver_enabled": {
          "description": "Show a wave-animation screensaver after `screensaver_idle_minutes` with no\ninput. Any key or mouse move ends it. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Display"
        },
        "screensaver_idle_minutes": {
          "description": "Idle minutes before the screensaver starts. `0` disables it. Default: 5",
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 5,
          "x-section": "Display"
        },
        "menu_bar_mnemonics": {
          "description": "Alt+letter opens menus (Alt+F for File, Alt+E for Edit, …). Turn off to free\nAlt+letter keys for other bindings. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "show_tab_bar": {
          "description": "Show the tab bar of open files in each pane. Can be toggled at any time.\nDefault: true",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "show_status_bar": {
          "description": "Show the status bar at the bottom. Can be toggled at any time.\nDefault: true",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "status_bar": {
          "description": "Which elements appear in the status bar and in what order.",
          "$ref": "#/$defs/StatusBarConfig",
          "default": {
            "left": [
              "{trust}",
              "{remote}",
              "{terminal_restart}",
              "{cursor}",
              "{diagnostics}",
              "{cursor_count}",
              "{messages}"
            ],
            "right": [
              "{read_only}",
              "{line_ending}",
              "{encoding}",
              "{language}",
              "{lsp}",
              "{warnings}",
              "{update}",
              "{palette}"
            ],
            "separator": ""
          },
          "x-section": "Status Bar"
        },
        "search": {
          "description": "Starting state of the Case / Word / Regex search toggles. Toggles you\nchange are remembered per workspace and take priority.",
          "$ref": "#/$defs/SearchConfig",
          "default": {
            "case_sensitive": false,
            "whole_word": false,
            "regex": false,
            "confirm_each": false
          },
          "x-section": "Search"
        },
        "show_prompt_line": {
          "description": "Always keep the bottom prompt line (search, open file, …) visible.\nWhen off (default), it only appears while a prompt is open.",
          "type": "boolean",
          "default": false,
          "x-section": "Display"
        },
        "show_vertical_scrollbar": {
          "description": "Show a vertical scrollbar in each pane. Can be toggled at any time.\nDefault: true",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "show_horizontal_scrollbar": {
          "description": "Show a horizontal scrollbar in each pane when line wrap is off and lines\nare wider than the window. Can be toggled at any time. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Display"
        },
        "show_tilde": {
          "description": "Show vim-style `~` markers on empty lines past the end of the file.\nDefault: true",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "nerd_font_icons": {
          "description": "Use Nerd Font icons in the UI (e.g. settings category icons). Only turn on\nif your terminal uses a Nerd Font, or icons show as `?` or boxes. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Display"
        },
        "use_terminal_bg": {
          "description": "Use the terminal's background instead of the theme's, so terminal\ntransparency or custom backgrounds show through. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Display"
        },
        "set_window_title": {
          "description": "Set the terminal window title to \"<file> — Fresh\" for the active buffer.\nDefault: true",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "terminal_auto_title": {
          "description": "Name terminal tabs after the running program (e.g. `python3`) and its title,\ninstead of the fixed `*Terminal N*`. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Display"
        },
        "cursor_style": {
          "description": "Cursor shape: block, bar or underline, blinking or steady.\nDefault: the terminal's own style.",
          "$ref": "#/$defs/CursorStyle",
          "default": "default",
          "x-section": "Display"
        },
        "rulers": {
          "description": "Draw vertical lines at these columns, e.g. [80, 120]. Default: none.\nColumns count screen cells from 1, so tabs and wide characters (CJK, emoji)\ncan differ from the status bar's column.",
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint",
            "minimum": 1
          },
          "default": [],
          "x-section": "Display"
        },
        "indentation_guide": {
          "description": "Vertical lines at each indent level (based on tab size). Display only.\n`none` (default): off. `all`: every level. `active`: only the cursor's block.",
          "$ref": "#/$defs/IndentationGuideMode",
          "default": "none",
          "x-section": "Display"
        },
        "indentation_guide_glyph": {
          "description": "Character for indentation guides; use a single-width one. Blank resets\nto the default. Default: ▏",
          "type": "string",
          "default": "▏",
          "x-section": "Display"
        },
        "rainbow_indentation": {
          "description": "Color indentation guides by level, using the theme's `indent_rainbow_1`–`6`\ncolors. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Display"
        },
        "whitespace_show": {
          "description": "Master switch for whitespace markers (·, →). When off, none are shown,\nwhatever the settings below say. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Whitespace"
        },
        "whitespace_spaces_leading": {
          "description": "Show · for spaces used as indentation. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Whitespace"
        },
        "whitespace_spaces_inner": {
          "description": "Show · for spaces between words. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Whitespace"
        },
        "whitespace_spaces_trailing": {
          "description": "Show · for spaces at the end of a line. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Whitespace"
        },
        "whitespace_tabs_leading": {
          "description": "Show → for tabs used as indentation. Languages can override this with\n`show_whitespace_tabs`. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Whitespace"
        },
        "whitespace_tabs_inner": {
          "description": "Show → for tabs between words. Languages can override this with\n`show_whitespace_tabs`. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Whitespace"
        },
        "whitespace_tabs_trailing": {
          "description": "Show → for tabs at the end of a line. Languages can override this with\n`show_whitespace_tabs`. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Whitespace"
        },
        "whitespace_newlines": {
          "description": "Show ↵ at the end of every line. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Whitespace"
        },
        "whitespace_carriage_returns": {
          "description": "Show ␍ for the CR in CRLF line endings. Stray CR characters always show\nas `<0D>`. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Whitespace"
        },
        "whitespace_in_selection": {
          "description": "Show whitespace markers (·, →) inside a selection, even if they are\nhidden elsewhere. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Whitespace"
        },
        "use_tabs": {
          "description": "Tab key inserts a tab character instead of spaces. Languages can\noverride this. Default: false (spaces)",
          "type": "boolean",
          "default": false,
          "x-section": "Editing"
        },
        "tab_size": {
          "description": "Spaces per tab. `0` means the default (4).",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 4,
          "x-section": "Editing"
        },
        "auto_indent": {
          "description": "Indent new lines to match the previous line.",
          "type": "boolean",
          "default": true,
          "x-section": "Editing"
        },
        "auto_close": {
          "description": "Typing `(`, `[`, `{`, `\"`, `'` or `` ` `` inserts the closing one too. Typing a\ncloser skips over an existing one; Backspace between a pair deletes both. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Editing"
        },
        "auto_surround": {
          "description": "Typing `(`, `[`, `{`, `\"`, `'` or `` ` `` with text selected wraps the\nselection instead of replacing it. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Editing"
        },
        "virtual_space": {
          "description": "Let the cursor go past the end of a line. \"off\" (default): never.\n\"block\": only block selections. \"on\": also arrows and clicks; typing there fills with spaces.",
          "$ref": "#/$defs/VirtualSpaceMode",
          "default": "off",
          "x-section": "Editing"
        },
        "scroll_offset": {
          "description": "Lines to keep visible above and below the cursor when scrolling.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 3,
          "x-section": "Editing"
        },
        "default_line_ending": {
          "description": "Line ending for new files; opened files keep their own. \"lf\" (Unix/macOS,\ndefault), \"crlf\" (Windows) or \"cr\" (classic Mac).",
          "$ref": "#/$defs/LineEndingOption",
          "default": "lf",
          "x-section": "Editing"
        },
        "trim_trailing_whitespace_on_save": {
          "description": "Remove spaces at line ends when saving. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Editing"
        },
        "ensure_final_newline_on_save": {
          "description": "Add a final newline when saving, if missing. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Editing"
        },
        "auto_read_only": {
          "description": "Open files read-only if they aren't writable or are in a library folder\n(node_modules, rustup, /usr/include, /nix/store, …). Binary files are\nalways read-only. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Editing"
        },
        "highlight_matching_brackets": {
          "description": "Highlight the matching bracket when the cursor is on a bracket. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Bracket Matching"
        },
        "rainbow_brackets": {
          "description": "Color nested brackets by depth. Needs `highlight_matching_brackets`. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Bracket Matching"
        },
        "completion_popup_auto_show": {
          "description": "Show the completion popup automatically while typing. When off (default),\nit only opens on request (e.g. Ctrl+Space).",
          "type": "boolean",
          "default": false,
          "x-section": "Completion"
        },
        "quick_suggestions": {
          "description": "Suggest completions on any typing, not just after `.` or `::`.\nNeeds `completion_popup_auto_show`. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Completion"
        },
        "quick_suggestions_delay_ms": {
          "description": "Milliseconds to wait before showing suggestions. Trigger characters like `.`\nskip the wait. Default: 150",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 150,
          "x-section": "Completion"
        },
        "suggest_on_trigger_characters": {
          "description": "Show completions right away after `.`, `::` or `->`, with no delay. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Completion"
        },
        "enable_inlay_hints": {
          "description": "Show inline type and parameter hints from the language server.",
          "type": "boolean",
          "default": true,
          "x-section": "LSP"
        },
        "enable_semantic_tokens_full": {
          "description": "Request semantic highlighting for the whole file, not just the visible\nrange. Default: false (lighter).",
          "type": "boolean",
          "default": false,
          "x-section": "LSP"
        },
        "diagnostics_inline_text": {
          "description": "Show the most severe error/warning message at the end of its line. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Diagnostics"
        },
        "mouse_wheel_scroll_lines": {
          "description": "Lines scrolled per mouse wheel notch, everywhere (minimum 1). Shift+wheel\nis not affected. Default: 3",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 3,
          "x-section": "Mouse"
        },
        "smooth_scroll": {
          "description": "Scroll a wheel notch one line at a time instead of jumping. Slow terminals\nfall back to a jump. Needs `animations` on.",
          "type": "boolean",
          "default": true,
          "x-section": "Mouse"
        },
        "mouse_hover_enabled": {
          "description": "Show documentation when hovering the mouse over code.\nOn Windows it also turns on full mouse-motion tracking, which may garble input\non some systems. Default: true (macOS/Linux), false (Windows)",
          "type": "boolean",
          "default": true,
          "x-section": "Mouse"
        },
        "mouse_hover_delay_ms": {
          "description": "Milliseconds before hover info appears. Lower is faster but loads the\nlanguage server more. Default: 500",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 500,
          "x-section": "Mouse"
        },
        "double_click_time_ms": {
          "description": "Max milliseconds between two clicks to count as a double-click. Default: 500",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 500,
          "x-section": "Mouse"
        },
        "auto_save_enabled": {
          "description": "Auto-save changed files to disk every `auto_save_interval_secs`. Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Recovery"
        },
        "auto_save_interval_secs": {
          "description": "Seconds between auto-saves, when `auto_save_enabled` is on. Default: 30",
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 30,
          "x-section": "Recovery"
        },
        "hot_exit": {
          "description": "Keep unsaved changes, including unnamed buffers, when you quit, and restore\nthem next time. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Recovery"
        },
        "confirm_quit": {
          "description": "Always ask before quitting, even with nothing unsaved. (Unsaved changes are\nalways confirmed.) Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Startup"
        },
        "restore_previous_session": {
          "description": "Restore tabs, splits, cursors and the file explorer from the last exit in\nthe same directory. The session is still saved when off; `--no-restore`\nskips both. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Startup"
        },
        "skip_session_restore_when_files_passed": {
          "description": "When started with files (e.g. `fresh main.rs`), open only those files\ninstead of the last session. Unsaved changes are still restored. Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Startup"
        },
        "ephemeral_file_patterns": {
          "description": "Files whose cursor, scroll and folds are never remembered, because other tools\nrewrite them. Plain names match exactly; `*`/`?` globs match the full path if they\ninclude folders, else the file name. Default: `[\"**/.git/**\"]` (e.g. `COMMIT_EDITMSG`)",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "**/.git/**"
          ],
          "x-section": "Startup"
        },
        "auto_create_empty_buffer_on_last_buffer_close": {
          "description": "Open an empty `[No Name]` tab when the last tab is closed. When off, the\nworkspace stays blank (with `file_explorer.auto_open_on_last_buffer_close`\nalso off, nothing opens). Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Startup"
        },
        "recovery_enabled": {
          "description": "Periodically save changes to recovery files, so work survives a crash.",
          "type": "boolean",
          "default": true,
          "x-section": "Recovery"
        },
        "auto_recovery_save_interval_secs": {
          "description": "Seconds between recovery saves, when `recovery_enabled` is on. Default: 2",
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 2,
          "x-section": "Recovery"
        },
        "auto_revert_poll_interval_ms": {
          "description": "How often (ms) to check open files for outside changes when auto-revert is\non. Lower is faster but uses more CPU. Default: 2000",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 2000,
          "x-section": "Recovery"
        },
        "keyboard_disambiguate_escape_codes": {
          "description": "Read Escape and modified keys reliably (kitty keyboard protocol, needs\nterminal support). Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Keyboard"
        },
        "keyboard_report_event_types": {
          "description": "Report key repeat and release events (kitty keyboard protocol, needs\nterminal support). Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Keyboard"
        },
        "keyboard_report_alternate_keys": {
          "description": "Report alternate keycodes as well as the base key (kitty keyboard protocol,\nneeds terminal support). Default: true",
          "type": "boolean",
          "default": true,
          "x-section": "Keyboard"
        },
        "keyboard_report_all_keys_as_escape_codes": {
          "description": "Report every key as an escape code; needed for repeat/release on plain\nkeys (kitty keyboard protocol, needs terminal support). Default: false",
          "type": "boolean",
          "default": false,
          "x-section": "Keyboard"
        },
        "snapshot_interval": {
          "description": "Edits between undo history snapshots.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 100,
          "x-section": "Performance"
        },
        "highlight_context_bytes": {
          "description": "Bytes around the visible area read for syntax highlighting. More is more\naccurate for long strings/comments but slower on big files. Default: 10000",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 10000,
          "x-section": "Performance"
        },
        "large_file_threshold_bytes": {
          "description": "Files over this size (bytes) count as large: they load lazily, skip\nlanguage servers, and get a fixed-size scrollbar thumb. Default: 10 MB",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 10485760,
          "x-section": "Performance"
        },
        "estimated_line_length": {
          "description": "Assumed average line length in bytes, used to estimate line positions in\nlarge files. Typical: 80–120.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 80,
          "x-section": "Performance"
        },
        "read_concurrency": {
          "description": "Max parallel file reads during bulk I/O. Higher helps on remote\nfilesystems. Default: 64",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 64,
          "x-section": "Performance"
        },
        "file_tree_poll_interval_ms": {
          "description": "How often (ms) the file explorer checks open folders for added or removed\nfiles. Lower is faster but uses more CPU. Default: 3000",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 3000,
          "x-section": "Performance"
        }
      }
    },
    "StatusBarConfig": {
      "description": "Which elements appear in the status bar, split into a left and a right\ngroup. Elements can be freely reordered.\n\nExample: `{\"left\": [\"{filename}\", \"{cursor:compact}\"], \"right\": [\"{language}\", \"{encoding}\"]}`",
      "type": "object",
      "properties": {
        "left": {
          "description": "Elements on the left side of the status bar.\nDefault: [\"{trust}\", \"{remote}\", \"{terminal_restart}\", \"{cursor}\", \"{diagnostics}\", \"{cursor_count}\", \"{messages}\"]",
          "type": "array",
          "items": {
            "$ref": "#/$defs/StatusBarElement"
          },
          "default": [
            "{trust}",
            "{remote}",
            "{terminal_restart}",
            "{cursor}",
            "{diagnostics}",
            "{cursor_count}",
            "{messages}"
          ],
          "x-section": "Status Bar",
          "x-dual-list-sibling": "/editor/status_bar/right",
          "x-dynamically-extendable-status-bar-elements": true
        },
        "right": {
          "description": "Elements on the right side of the status bar.\nDefault: [\"{read_only}\", \"{line_ending}\", \"{encoding}\", \"{language}\", \"{lsp}\", \"{warnings}\", \"{update}\", \"{palette}\"]",
          "type": "array",
          "items": {
            "$ref": "#/$defs/StatusBarElement"
          },
          "default": [
            "{read_only}",
            "{line_ending}",
            "{encoding}",
            "{language}",
            "{lsp}",
            "{warnings}",
            "{update}",
            "{palette}"
          ],
          "x-section": "Status Bar",
          "x-dual-list-sibling": "/editor/status_bar/left",
          "x-dynamically-extendable-status-bar-elements": true
        },
        "separator": {
          "description": "Text drawn between status bar elements, used as-is. Each element already\nhas a one-space margin, so `\"|\"` shows as `LF | UTF-8`. Empty (default) means no separator.",
          "type": "string",
          "default": "",
          "x-section": "Status Bar"
        }
      }
    },
    "StatusBarElement": {
      "type": "string",
      "x-dual-list-options": [
        {
          "value": "{filename}",
          "name": "Filename"
        },
        {
          "value": "{read_only}",
          "name": "Read-Only"
        },
        {
          "value": "{cursor}",
          "name": "Cursor"
        },
        {
          "value": "{cursor:compact}",
          "name": "Cursor (compact)"
        },
        {
          "value": "{diagnostics}",
          "name": "Diagnostics"
        },
        {
          "value": "{cursor_count}",
          "name": "Cursor Count"
        },
        {
          "value": "{messages}",
          "name": "Messages"
        },
        {
          "value": "{chord}",
          "name": "Chord"
        },
        {
          "value": "{line_ending}",
          "name": "Line Ending"
        },
        {
          "value": "{encoding}",
          "name": "Encoding"
        },
        {
          "value": "{language}",
          "name": "Language"
        },
        {
          "value": "{lsp}",
          "name": "LSP"
        },
        {
          "value": "{warnings}",
          "name": "Warnings"
        },
        {
          "value": "{update}",
          "name": "Update"
        },
        {
          "value": "{terminal_restart}",
          "name": "Terminal Restart"
        },
        {
          "value": "{palette}",
          "name": "Palette"
        },
        {
          "value": "{clock}",
          "name": "Clock"
        },
        {
          "value": "{remote}",
          "name": "Remote Indicator"
        },
        {
          "value": "{trust}",
          "name": "Workspace Trust"
        }
      ]
    },
    "SearchConfig": {
      "description": "Starting state of the search toggles in every search UI (search/replace\nprompt, Live Grep, Search & Replace panel).",
      "type": "object",
      "properties": {
        "case_sensitive": {
          "description": "Match case (default: false, so `todo` also finds `TODO`). Flipping the\ntoggle overrides this and is saved with the workspace.",
          "type": "boolean",
          "default": false
        },
        "whole_word": {
          "description": "Match whole words only (default: false).",
          "type": "boolean",
          "default": false
        },
        "regex": {
          "description": "Treat the query as a regular expression (default: false).",
          "type": "boolean",
          "default": false
        },
        "confirm_each": {
          "description": "Ask before each replacement (default: false).",
          "type": "boolean",
          "default": false
        }
      }
    },
    "CursorStyle": {
      "description": "Terminal cursor style",
      "type": "string",
      "enum": [
        "default",
        "blinking_block",
        "steady_block",
        "blinking_bar",
        "steady_bar",
        "blinking_underline",
        "steady_underline"
      ]
    },
    "IndentationGuideMode": {
      "description": "Indentation guide rendering mode.",
      "type": "string",
      "enum": [
        "none",
        "all",
        "active"
      ],
      "default": "none"
    },
    "VirtualSpaceMode": {
      "description": "Where the cursor may move beyond the end of a line",
      "type": "string",
      "enum": [
        "off",
        "block",
        "on"
      ],
      "default": "off"
    },
    "LineEndingOption": {
      "description": "Default line ending format for new files",
      "type": "string",
      "enum": [
        "lf",
        "crlf",
        "cr"
      ],
      "default": "lf"
    },
    "FileExplorerConfig": {
      "description": "File explorer settings.",
      "type": "object",
      "properties": {
        "respect_gitignore": {
          "description": "Apply `.gitignore` rules. When off, ignored files are neither hidden nor\ngrayed out. Default: true",
          "type": "boolean",
          "default": true
        },
        "show_hidden": {
          "description": "Show hidden files (starting with `.`) by default.",
          "type": "boolean",
          "default": false
        },
        "show_gitignored": {
          "description": "Show gitignored files by default.",
          "type": "boolean",
          "default": false
        },
        "custom_ignore_patterns": {
          "description": "Extra patterns to ignore, on top of `.gitignore`.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "width": {
          "description": "Explorer width: a percent (`\"30%\"`, 0–100) or a column count (`\"24\"`).\nA plain number is read as a percent; `0.3` means 30%.",
          "$ref": "#/$defs/ExplorerWidth",
          "default": "30%"
        },
        "preview_tabs": {
          "description": "A single click opens a temporary preview tab that the next click replaces.\nEditing, double-click, Enter or dragging the tab makes it permanent. Default: true",
          "type": "boolean",
          "default": true
        },
        "side": {
          "description": "Screen side for the file explorer. Default: left",
          "$ref": "#/$defs/FileExplorerSide",
          "default": "left"
        },
        "follow_active_buffer": {
          "description": "Reveal and select the current file in the tree whenever you switch files.\nSkipped while the sidebar is hidden or focused, and for files outside the\nproject. Default: false",
          "type": "boolean",
          "default": false
        },
        "auto_open_on_last_buffer_close": {
          "description": "Focus the file explorer when the last tab is closed. Turn off for a blank\nworkspace where nothing opens by itself. Default: true",
          "type": "boolean",
          "default": true
        },
        "compact_directories": {
          "description": "Show chains of folders that each hold only one folder on one line, e.g.\n`src/main/java/com/example`. Default: true",
          "type": "boolean",
          "default": true
        },
        "tree_indicator_collapsed": {
          "description": "Symbol before a closed folder (one character recommended). Default: \">\"",
          "type": "string",
          "default": ">"
        },
        "tree_indicator_expanded": {
          "description": "Symbol before an open folder (one character recommended). Default: \"▼\"",
          "type": "string",
          "default": "▼"
        }
      }
    },
    "ExplorerWidth": {
      "description": "Either a percent like \"30%\" (0–100) or an absolute column count like \"24\".",
      "type": "string",
      "pattern": "^(100%|[1-9]?[0-9]%|\\d+)$"
    },
    "FileExplorerSide": {
      "description": "Side placement for the file explorer panel.",
      "type": "string",
      "enum": [
        "left",
        "right"
      ]
    },
    "SidebarConfig": {
      "description": "The sidebar column: the file explorer plus any plugin sections below it.",
      "type": "object",
      "properties": {
        "accordion": {
          "description": "Open sections freely (`free`, default) or one at a time (`exclusive`).",
          "$ref": "#/$defs/SidebarAccordion",
          "default": "free"
        }
      }
    },
    "SidebarAccordion": {
      "description": "How the sidebar's sections share the column.",
      "oneOf": [
        {
          "description": "Any number of sections can be open; closing one gives its space to the others.",
          "type": "string",
          "const": "free"
        },
        {
          "description": "Only one section open at a time; dividers can't be dragged.",
          "type": "string",
          "const": "exclusive"
        }
      ]
    },
    "FileBrowserConfig": {
      "description": "Open File dialog settings.",
      "type": "object",
      "properties": {
        "show_hidden": {
          "description": "Show hidden files (starting with `.`) by default.",
          "type": "boolean",
          "default": false
        }
      }
    },
    "ClipboardConfig": {
      "description": "Which clipboard methods copy/paste uses, tried in order: OSC 52 (modern\nterminals like Kitty, WezTerm), the system clipboard (X11/Wayland), then an\ninternal clipboard. Disable a method if it hangs (e.g. PuTTY, some SSH setups).",
      "type": "object",
      "properties": {
        "use_osc52": {
          "description": "Use the terminal's OSC 52 clipboard (default: true). Turn off if unsupported\nor it hangs.",
          "type": "boolean",
          "default": true
        },
        "use_system_clipboard": {
          "description": "Use the X11/Wayland system clipboard (default: true). Turn off if there's\nno display server or it causes problems.",
          "type": "boolean",
          "default": true
        }
      }
    },
    "TerminalConfig": {
      "description": "Integrated terminal settings.",
      "type": "object",
      "properties": {
        "jump_to_end_on_output": {
          "description": "Jump back to the live terminal when new output arrives while you're in\nscrollback (default: true).",
          "type": "boolean",
          "default": true
        },
        "shell": {
          "description": "Shell for the terminal. Unset (default) uses `$SHELL` or the system default.\nDoesn't change `$SHELL` for other features, and doesn't apply to remote\nor container terminals.",
          "anyOf": [
            {
              "$ref": "#/$defs/TerminalShellConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "skip_app_execution_alias": {
          "description": "Windows only: when picking a shell, skip Microsoft Store app aliases, which\ncan crash on start. Turn off to use the first `pwsh.exe` on `PATH`.\nIgnored if `terminal.shell` is set. Default: true",
          "type": "boolean",
          "default": true
        },
        "resume_agents": {
          "description": "When restoring Orchestrator agent sessions, resume the previous\nconversation (e.g. `claude --resume`) instead of starting clean. Default: true",
          "type": "boolean",
          "default": true
        },
        "mouse_drag_selects": {
          "description": "Dragging in a terminal selects text for Ctrl+C, switching to scrollback\n(Ctrl+Space returns). A click only focuses. Default: true",
          "type": "boolean",
          "default": true
        },
        "mouse_forwarding": {
          "description": "When programs in the terminal get mouse events. `requested` (default):\nonly programs that ask for the mouse (plus wheel for full-screen apps);\nShift+drag still selects. `alt_screen`: every full-screen program gets all events.",
          "$ref": "#/$defs/TerminalMouseForwarding",
          "default": "requested"
        }
      }
    },
    "TerminalShellConfig": {
      "description": "Shell command and arguments for the terminal.",
      "type": "object",
      "properties": {
        "command": {
          "description": "Program to run (e.g. `/usr/bin/fish`, `bash`, or a script). Looked up in\n`$PATH` if not absolute.",
          "type": "string"
        },
        "args": {
          "description": "Arguments for the shell.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        }
      },
      "required": [
        "command"
      ]
    },
    "TerminalMouseForwarding": {
      "description": "When programs in the terminal get mouse events.",
      "oneOf": [
        {
          "description": "Only to programs that ask for the mouse; Shift+drag still selects.",
          "type": "string",
          "const": "requested"
        },
        {
          "description": "Old behavior: all events to any full-screen program.",
          "type": "string",
          "const": "alt_screen"
        }
      ]
    },
    "Keybinding": {
      "description": "Keybinding definition",
      "type": "object",
      "properties": {
        "key": {
          "description": "Key name (e.g. \"a\", \"Enter\", \"F1\") for a single-key binding.",
          "type": "string"
        },
        "modifiers": {
          "description": "Modifiers (e.g. [\"ctrl\"], [\"ctrl\", \"shift\"]) for a single-key binding.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "keys": {
          "description": "Key sequence for a chord, e.g. [{\"key\": \"x\", \"modifiers\": [\"ctrl\"]}, {\"key\": \"s\", \"modifiers\": [\"ctrl\"]}].\nOverrides `key` + `modifiers`.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/KeyPress"
          }
        },
        "chord": {
          "description": "The whole binding as one string, e.g. `\"C-x\"`, `\"C-S-Left\"`, `\"C-x C-s\"`.\n`key` + `modifiers` and `keys` win if set.",
          "type": "string"
        },
        "action": {
          "description": "Action to run (e.g. \"move_left\"), or \"unbind\" to remove the built-in binding for this key.",
          "type": "string"
        },
        "args": {
          "description": "Arguments for the action (optional).",
          "type": "object",
          "additionalProperties": true,
          "default": {}
        },
        "when": {
          "description": "Condition for when the binding applies (optional, e.g. \"mode == insert\").",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "action"
      ],
      "x-display-field": "/action"
    },
    "KeyPress": {
      "description": "A single key in a sequence",
      "type": "object",
      "properties": {
        "key": {
          "description": "Key name (e.g., \"a\", \"Enter\", \"F1\")",
          "type": "string"
        },
        "modifiers": {
          "description": "Modifiers (e.g., [\"ctrl\"], [\"ctrl\", \"shift\"])",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        }
      },
      "required": [
        "key"
      ]
    },
    "KeymapConfig": {
      "description": "A keymap (built-in or custom).",
      "type": "object",
      "properties": {
        "inherits": {
          "description": "Keymap to inherit from (optional).",
          "type": [
            "string",
            "null"
          ]
        },
        "bindings": {
          "description": "Keybindings in this keymap.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Keybinding"
          },
          "default": []
        }
      },
      "x-display-field": "/inherits"
    },
    "KeybindingMapOptions": {
      "description": "Available keybinding maps",
      "type": "string",
      "enum": [
        "default",
        "emacs",
        "vscode",
        "macos",
        "macos-gui"
      ]
    },
    "LanguageConfig": {
      "description": "Settings for one language.",
      "type": "object",
      "properties": {
        "extensions": {
          "description": "File extensions (e.g. [\"rs\"] for Rust).",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "filenames": {
          "description": "Exact file names (e.g. [\"Makefile\", \"GNUmakefile\"]).",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "grammar": {
          "description": "Tree-sitter grammar name.",
          "type": "string",
          "default": ""
        },
        "comment_prefix": {
          "description": "Line comment prefix (e.g. \"//\").",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "auto_indent": {
          "description": "Auto-indent new lines.",
          "type": "boolean",
          "default": true
        },
        "auto_close": {
          "description": "Auto-close brackets and quotes. `null` uses `editor.auto_close`.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "auto_surround": {
          "description": "Wrap selections in typed brackets/quotes. `null` uses `editor.auto_surround`.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "textmate_grammar": {
          "description": "Custom grammar file for files with this language's extensions (optional).\nMust be a Sublime Text `.sublime-syntax` file; `.tmLanguage` is not supported.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "show_whitespace_tabs": {
          "description": "Show → for tabs (default: true). Turn off for tab-indented languages like Go.",
          "type": "boolean",
          "default": true
        },
        "line_wrap": {
          "description": "Wrap long lines (e.g. for Markdown). `null` uses `editor.line_wrap`.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "wrap_column": {
          "description": "Number of text columns to wrap lines at, not counting the line-number\ngutter. `null` or `0` uses `editor.wrap_column`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null
        },
        "page_view": {
          "description": "Open in page view: centered text, hidden formatting marks, smart wrapping.\n`null` means off.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "page_width": {
          "description": "Text width in columns in page view. `null` or `0` uses `editor.page_width`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null
        },
        "use_tabs": {
          "description": "Tab key inserts a tab character (e.g. for Go, Makefile). `null` uses `editor.use_tabs`.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "tab_size": {
          "description": "Spaces per tab. `null` or `0` uses `editor.tab_size`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null
        },
        "formatter": {
          "description": "Formatter used by the Format Buffer command.",
          "anyOf": [
            {
              "$ref": "#/$defs/FormatterConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "format_on_save": {
          "description": "Run the formatter on save.",
          "type": "boolean",
          "default": false
        },
        "on_save": {
          "description": "Commands to run on save (e.g. linters), in order; stops at the first failure.\nFor formatting, use `formatter` + `format_on_save` instead.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/OnSaveAction"
          },
          "default": []
        },
        "word_characters": {
          "description": "Extra characters (besides letters, digits and `_`) that count as part of a\nword for word completion, e.g. `\"-\"` for Lisp/CSS, `\"$\"` for PHP/Bash, `\"?!\"` for Ruby.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "indent": {
          "description": "Custom auto-indent rules. Unset patterns keep the built-in ones. Works\nwithout a tree-sitter grammar.",
          "anyOf": [
            {
              "$ref": "#/$defs/IndentRulesConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "indentation_guide": {
          "description": "Show indentation guides. `null` follows `editor.indentation_guide`,\nexcept plain text, where guides are off unless set to `true`.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        }
      },
      "x-display-field": "/grammar"
    },
    "FormatterConfig": {
      "description": "Formatter for a language.",
      "type": "object",
      "properties": {
        "command": {
          "description": "Formatter command (e.g. \"rustfmt\", \"prettier\").",
          "type": "string"
        },
        "args": {
          "description": "Arguments for the formatter. \"$FILE\" is replaced by the file path.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "stdin": {
          "description": "Send the text on stdin and read the result from stdout (default: true).",
          "type": "boolean",
          "default": true
        },
        "timeout_ms": {
          "description": "Timeout in milliseconds (default: 10000)",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 10000
        }
      },
      "required": [
        "command"
      ],
      "x-display-field": "/command"
    },
    "OnSaveAction": {
      "description": "Command to run when a file is saved (e.g. a linter).",
      "type": "object",
      "properties": {
        "command": {
          "description": "Shell command to run. The file path is available as $FILE.",
          "type": "string"
        },
        "args": {
          "description": "Arguments for the command. \"$FILE\" is replaced by the file path.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "working_dir": {
          "description": "Working directory (default: project root).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "stdin": {
          "description": "Send the file's text on stdin.",
          "type": "boolean",
          "default": false
        },
        "timeout_ms": {
          "description": "Timeout in milliseconds (default: 10000)",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 10000
        },
        "enabled": {
          "description": "Set to false to turn the action off without removing it (default: true).",
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "command"
      ],
      "x-display-field": "/command"
    },
    "IndentRulesConfig": {
      "description": "Auto-indent rules used when you press Enter. Each is an optional regex (no\nlook-around or back-references), matched with strings and comments ignored.\nUnset patterns keep the language's built-in rules.",
      "type": "object",
      "properties": {
        "increase_indent_pattern": {
          "description": "If the current line matches, the new line is indented one level deeper.\nE.g. `[\\{\\[\\(]\\s*$` (ends with an open bracket) or `:\\s*$` (Python).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "decrease_indent_pattern": {
          "description": "If a line matches, it is dedented one level, also while typing.\nE.g. `^\\s*[\\}\\]\\)]` (starts with a closing bracket). Include terminators\nlike Python's `:` so words starting with a keyword don't dedent.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "indent_next_line_pattern": {
          "description": "Like `increase_indent_pattern`, but only for the next line.\nE.g. `^\\s*(if|for|while)\\b.*\\)\\s*$` (an `if` without braces).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "dedent_next_line_pattern": {
          "description": "If the current line matches, the next line is dedented one level.\nE.g. `^\\s*(return|pass|raise|break|continue)\\b` (Python).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "self_close_pattern": {
          "description": "Cancels `increase_indent_pattern` when the line also closes its block,\ne.g. Ruby `\\bend\\b` for `def f; end`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "LspLanguageConfig": {
      "description": "One or more LSP server configs for this language.\nAccepts both a single object and an array for backwards compatibility.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/LspServerConfig"
      }
    },
    "LspServerConfig": {
      "description": "Language server settings.",
      "type": "object",
      "properties": {
        "command": {
          "description": "Command that starts the server. Required when enabled.",
          "type": "string",
          "default": "",
          "x-order": 1
        },
        "enabled": {
          "description": "Enable this server.",
          "type": "boolean",
          "default": true,
          "x-order": 2
        },
        "name": {
          "description": "Display name (e.g. \"tsserver\"). Defaults to the command's file name.",
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "x-order": 3
        },
        "args": {
          "description": "Arguments for the server. If omitted, the default server's arguments are\nused; any list, even `[]`, replaces them.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "default": null,
          "x-order": 4
        },
        "auto_start": {
          "description": "Start the server when a matching file opens (default: true). When off,\nstart it from the command palette.",
          "type": "boolean",
          "default": true,
          "x-order": 5
        },
        "root_markers": {
          "description": "Files or folders that mark the project root: the nearest folder above the\nfile that contains one is used. Empty means `[\".git\"]`. With no match, the\nfile's own folder is used.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "x-order": 6
        },
        "env": {
          "description": "Extra environment variables for the server (override inherited ones).",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "x-section": "Advanced",
          "x-order": 10
        },
        "language_id_overrides": {
          "description": "Language ID to send to the server per file extension (no dot),\ne.g. `{\"tsx\": \"typescriptreact\"}`.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "x-section": "Advanced",
          "x-order": 11
        },
        "initialization_options": {
          "description": "Server-specific `initializationOptions` sent at startup.",
          "default": null,
          "x-section": "Advanced",
          "x-order": 12
        },
        "only_features": {
          "description": "Use this server only for these features. Don't combine with\n`except_features`; if neither is set, it handles everything.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/LspFeature"
          },
          "default": null,
          "x-section": "Advanced",
          "x-order": 13
        },
        "except_features": {
          "description": "Use this server for everything except these features. Don't combine with\n`only_features`.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/LspFeature"
          },
          "default": null,
          "x-section": "Advanced",
          "x-order": 14
        },
        "process_limits": {
          "description": "Memory and CPU limits for the server.",
          "$ref": "#/$defs/ProcessLimits",
          "default": {
            "max_memory_percent": 50,
            "max_cpu_percent": 90,
            "enabled": true
          },
          "x-section": "Advanced",
          "x-order": 15
        }
      },
      "x-display-field": "/command"
    },
    "LspFeature": {
      "description": "Language server feature, for routing features to servers when a language has\nseveral. \"Merged\" features combine results from all servers; \"exclusive\" ones use the first.",
      "oneOf": [
        {
          "description": "Diagnostics (merged)",
          "type": "string",
          "const": "diagnostics"
        },
        {
          "description": "Code completion (merged)",
          "type": "string",
          "const": "completion"
        },
        {
          "description": "Code actions / quick fixes (merged)",
          "type": "string",
          "const": "code_action"
        },
        {
          "description": "Document symbols (merged)",
          "type": "string",
          "const": "document_symbols"
        },
        {
          "description": "Workspace symbols (merged)",
          "type": "string",
          "const": "workspace_symbols"
        },
        {
          "description": "Hover information (exclusive)",
          "type": "string",
          "const": "hover"
        },
        {
          "description": "Go to definition, declaration, type definition (exclusive)",
          "type": "string",
          "const": "definition"
        },
        {
          "description": "Go to implementation (exclusive)",
          "type": "string",
          "const": "implementation"
        },
        {
          "description": "Find references (exclusive)",
          "type": "string",
          "const": "references"
        },
        {
          "description": "Document formatting and range formatting (exclusive)",
          "type": "string",
          "const": "format"
        },
        {
          "description": "Rename and prepare rename (exclusive)",
          "type": "string",
          "const": "rename"
        },
        {
          "description": "Signature help (exclusive)",
          "type": "string",
          "const": "signature_help"
        },
        {
          "description": "Inlay hints (exclusive)",
          "type": "string",
          "const": "inlay_hints"
        },
        {
          "description": "Folding ranges (exclusive)",
          "type": "string",
          "const": "folding_range"
        },
        {
          "description": "Semantic tokens (exclusive)",
          "type": "string",
          "const": "semantic_tokens"
        },
        {
          "description": "Document highlight (exclusive)",
          "type": "string",
          "const": "document_highlight"
        }
      ]
    },
    "ProcessLimits": {
      "description": "Resource limits for a process.",
      "type": "object",
      "properties": {
        "max_memory_percent": {
          "description": "Max memory as a percent of system memory (default: 50). `null` means no limit.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "default": null
        },
        "max_cpu_percent": {
          "description": "Max CPU as a percent, where 100 = one core, 200 = two cores. `null` means no limit.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "default": null
        },
        "enabled": {
          "description": "Apply these limits. Default: true (the built-in config turns them on\nonly on Linux).",
          "type": "boolean",
          "default": true
        }
      }
    },
    "WarningsConfig": {
      "description": "Warning notification settings.",
      "type": "object",
      "properties": {
        "show_status_indicator": {
          "description": "Show a colored status-bar indicator for errors and warnings (default: true).",
          "type": "boolean",
          "default": true
        }
      }
    },
    "PluginConfig": {
      "description": "Settings for one plugin.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Load and run this plugin (default: true).",
          "type": "boolean",
          "default": true
        },
        "path": {
          "description": "Plugin file path. Filled in automatically; don't set it by hand.",
          "type": [
            "string",
            "null"
          ],
          "readOnly": true
        },
        "settings": {
          "description": "The plugin's own settings, as defined by its `<plugin_name>.schema.json`.\nShown in Settings as the plugin's page under \"Plugins\".",
          "readOnly": true
        }
      },
      "x-display-field": "/enabled"
    },
    "PackagesConfig": {
      "description": "Package manager settings for plugins and themes.",
      "type": "object",
      "properties": {
        "sources": {
          "description": "Git repository URLs of plugin/theme registries.\nDefault: [\"https://github.com/sinelaw/fresh-plugins-registry\"]",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "https://github.com/sinelaw/fresh-plugins-registry"
          ]
        }
      }
    },
    "EnvConfig": {
      "description": "Which marker files identify a project environment and how to activate it.\nThe same markers drive the Workspace Trust prompt. You can add or override detectors.",
      "type": "object",
      "properties": {
        "detectors": {
          "description": "Detectors in order; the first that matches the workspace root wins.\nDefaults cover venv, direnv, mise, pipenv and poetry.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvDetector"
          },
          "default": [
            {
              "name": ".venv",
              "markers": [
                ".venv"
              ],
              "kind": "path-only",
              "snippet": "source .venv/bin/activate",
              "require": [
                ".venv/bin/python",
                ".venv/bin/python3",
                ".venv/Scripts/python.exe"
              ]
            },
            {
              "name": "venv",
              "markers": [
                "venv"
              ],
              "kind": "path-only",
              "snippet": "source venv/bin/activate",
              "require": [
                "venv/bin/python",
                "venv/bin/python3",
                "venv/Scripts/python.exe"
              ]
            },
            {
              "name": "direnv",
              "markers": [
                ".envrc"
              ],
              "kind": "shell",
              "snippet": "eval \"$(direnv export bash)\"",
              "require": []
            },
            {
              "name": "mise",
              "markers": [
                "mise.toml",
                ".mise.toml",
                ".tool-versions"
              ],
              "kind": "shell",
              "snippet": "eval \"$(mise env -s bash)\"",
              "require": []
            },
            {
              "name": "pipenv",
              "markers": [
                "Pipfile"
              ],
              "kind": "shell",
              "snippet": "source \"$(pipenv --venv)/bin/activate\"",
              "require": []
            },
            {
              "name": "poetry",
              "markers": [
                "poetry.lock"
              ],
              "kind": "shell",
              "snippet": "source \"$(poetry env info --path)/bin/activate\"",
              "require": []
            }
          ]
        }
      }
    },
    "EnvDetector": {
      "description": "One environment detector: its markers, risk, activation command and name.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Short label shown in the status pill (e.g. \".venv\", \"direnv\", \"mise\").",
          "type": "string"
        },
        "markers": {
          "description": "Files or folders at the workspace root; the detector matches if any exists.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "kind": {
          "description": "Activation risk class.",
          "$ref": "#/$defs/EnvKind"
        },
        "snippet": {
          "description": "Shell command that activates the environment, run from the workspace root.\nPrefer relative paths (e.g. `source .venv/bin/activate`). `{dir}` expands to the\nworkspace root, but avoid it unless you control the path (shell-injection risk).",
          "type": "string"
        },
        "require": {
          "description": "Paths (relative to the workspace root) of which at least one must also exist,\ne.g. the Python interpreter inside `.venv`. Empty means markers are enough.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        }
      },
      "required": [
        "name",
        "markers",
        "kind",
        "snippet"
      ],
      "x-display-field": "/name"
    },
    "EnvKind": {
      "description": "How risky activation is; decides whether a trust prompt is needed first.",
      "oneOf": [
        {
          "description": "Only changes `PATH` or a few variables (e.g. a Python virtualenv). Low risk:\nactivates silently once trusted; a folder with only this kind needs no prompt.",
          "type": "string",
          "const": "path-only"
        },
        {
          "description": "Runs project shell code (e.g. `direnv export`, `mise env`). Only after the workspace is trusted.",
          "type": "string",
          "const": "shell"
        }
      ]
    }
  }
}
