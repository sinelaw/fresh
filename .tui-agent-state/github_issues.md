# GitHub Issues Index

This is the canonical reference for every GitHub issue this agent has filed.
**Check this file BEFORE searching GitHub or filing any new issue.**
If a topic appears here — open or closed — do not file a duplicate.

Last updated: Run #61, 2026-09-02 (master still v0.4.10 but FORCE-UPDATED @ **7bcc8ff6c** — ~50 retained-mode render/input commits; no maintainer action on the 5 open agent issues, no rechecks due. **Filed #3136** (med-high): terminal input replay — a command typed before opening the Run Agent dialog is re-injected as pending PTY input after a workspace switch away+back (Enter re-executes; 3/3 with dialog, controls clean). **Filed #3137** (med): Move-to-Folder popup keyboard regression — ↓ falls through to the dock (live workspace switch) and dismisses the popup; regression vs closed #2694; mouse workaround. Also: Run Agent/New Workspace unified dialog COMPREHENSIVE PASS; 0.4.10 Ctrl+B/Ctrl+E + explorer-leak key-routing fix VERIFIED; B8.2 terminal-tab extraction + B7.6 folder search/subfolders PASS. NEAR-FALSE-POSITIVE recorded: sticky custom agent command re-running in new workspaces masquerades as cross-workspace scrollback contamination — check the Agent row.)
Prev — Run #60, 2026-08-24 (master JUMPED v0.4.4→**v0.4.10 @ 992e61d02**; maintainer closed a big batch across 0.4.5–0.4.10, **open agent issues 13→4** — remaining: #2598, #2318, #2197, #2122, all off-emphasis. **#2735 CONFIRMED FIXED (0.4.9)** via UI, already maintainer-closed → recorded not re-commented: Extract-Tab co-tenant survives restart (both `.ws-*.json` written, both windows restore w/ their files) + `--no-restore` writes no session layout (only trust.json). **BUG #3074 FILED (med):** terminal tab NAME not preserved on editor restart — restored exited terminal shows generic `*Terminal N*` instead of `bash — <cwd>`; 2 dead terminals become indistinguishable; contradicts terminal.md §Persistence + 0.4.6 changelog. Also fully verified the **Restart-Exited-Terminal 0.4.6 flagship** feature COMPREHENSIVE PASS.)
Prev — Run #59, 2026-07-20 (master ADVANCED v0.4.3→**v0.4.4 @ f545a75ad** → preflight rechecks due; maintainer closed a LARGE batch, open agent issues **25→12**. **5 fixed issues CONFIRMED via UI** — all already maintainer-closed, recorded not re-commented: **#2135** Edit-menu Replace split (`Replace...`=Ctrl+R basic / `Query Replace...`=Ctrl+Alt+R interactive, functionally proven); **#2606** vi visual-block `>`/`<` indent + exit NORMAL; **#2604** `da"` single-space (cat-A); **#2591** Git Grep no-match → "No matches" no ⚠; **#2577** virtual-space status Col tracks cursor. **3 new 0.4.4 features PASS, no bug/no false positive:** Virtual Space (per-buffer, status tracks past-EOL, typing materializes padding), Save All ("Saved N files"/graceful empty), Search-in-Project Files field #2699 (`*.rs`/`src/**`/`*.py,*.md` globs). Side note for a future #2309 recheck: the *Search/Replace* panel buffer DOES render `[RO]` — #2309's missing-indicator was on FILE buffers, re-verify those. NO new issue filed this run.)
Run #59b (CONCURRENT supplementary, 2026-07-20): a second agent instance ran in parallel on the same v0.4.4 build. Unique/complementary work, no overlap with the batch above: **#2197 (pyright) RECHECKED on v0.4.4 — STILL BROKEN** (hover/definition/diagnostic 30s timeout, `position_encoding=None`, pull-diagnostics stall); posted dated confirmation comment 5019301875 (the batch run did not touch #2197 — it's open, not fixed). **`next_pane`/`prev_pane` (#2562, new v0.4.4)** black-box characterized (builtin, NO default key + NO palette command → keybinding-only, **IMP-032**; editor bindings 947); runtime cycling `[~]` harness-blocked — feeds Run #60 candidate (d). LESSON (both instances independently hit it): container can reclaim mid-run / a stale local ref hides history → always `git fetch origin <state-branch>` + compare HEAD before trusting "up to date".)
Run #59c (CONCURRENT, emphasis-area — user directive, 2026-07-20): User-directed basics→fancy sweep of the two EMPHASIS areas (embedded terminals + workspace orchestration; vi ignored). **Terminal mouse text selection (NEW 0.4.4) COMPREHENSIVE PASS** (drag→read-only+selection, Ctrl+C copy=exact paste, double-click word/triple-click line, bare-click focus-only, plain-drag forwarded to a DECSET-1000 program while Shift+drag bypasses+selects). **NextPane→terminal mode (#2562)** + **per-split scrollback (#2595)** PASS. **Ctrl+] exit-terminal-mode is a NO-OP** in plain tmux (0x1D→shell; pending, likely #2109-class, not filed). **Workspace folders (#2703) COMPREHENSIVE PASS** (create/rename-F2/delete/move/persist). **Tab extraction → co-tenant works live** but **filed #2735**: the extracted co-tenant workspace is NOT persisted on quit (only the source survives → lost on restart; contradicts the 0.4.4 "restore each co-tenant window" commit). Discovered the "Show empty/1-file workspaces (Alt+I)" default-OFF filter. Terminal scrollback reloads under `--no-restore` → pending, plausibly intended.)
Prev: Run #58, 2026-07-07 (master UNCHANGED @ **v0.4.3 6ab255709** since Run #57 → no rechecks due; 25 open agent issues untouched by maintainer. **LSP code folding first-ever systematic test: COMPREHENSIVE PASS** — LSP foldingRange + indent fallback both live (use-block ▾ only with LSP = litmus), Toggle Fold + gutter-click toggle, fold-from-within folds containing range, Up/Down skip, edits-above don't drift (#1571 holds), per-split state, folds+wrap, session restore + external-edit relocation-by-header (#1568 holds), nested fold memory, 12MB large-file lazy-mode folding all verified. **Commented on #2301** (3rd trigger: fold-from-within moves cursor to header, status keeps stale hidden Ln; typing edits header while status lies). Wrapped-header ellipsis placement → IMP-031. vi quick sweep: `d2fo`, `cc`/`S`, block `x` all PASS; filed **#2606**: visual-block (Ctrl+V) `>`/`<` indent operators are no-ops that also don't exit VISUAL BLOCK (V+`>` and block-`x` controls work) — residual gap of the #2438 family. slangd still absent.)
Prev: Run #57, 2026-07-07 (master FORCE-UPDATED to **v0.4.3 @ 6ab255709** — 3 new commits incl. two fixing open agent vi issues: **#2441 (find-char + operators, `f242ec149`) and #2438 (`>>`/`<<` + visual indent, `cbe2b412c`) CONFIRMED FIXED via UI, commented on both** (maintainer had closed them same day). Format Buffer first-ever test: whole-buffer external path byte-perfect (rustfmt), custom `formatter:{command,args}` works (marker script proven, undoable buffer edit, disk untouched), graceful missing-binary + no-LSP errors. Filed **#2605**: Format Buffer ignores the active selection (whole-file reformat) and the documented LSP formatting fallback incl. range formatting is unreachable when a default formatter exists (`null` can't unset; `""`/missing command error without fallback). Run #56's two.rs pending item does NOT reproduce (new `src/bin/two.rs` created mid-session got full diagnostics: publish ~2s post-save, gutter ●, F8, clears-on-fix) — closed as likely transient, re-characterize only if seen again. Docs nit → IMP-030: configuration/index.md `"formatter": "prettier --write"` string example is INVALID (`expected struct FormatterConfig`) and discards the whole config layer; struct form documented only in plugins/development/language-packs.md. slangd still absent.)
Prev: Run #56, 2026-07-07 (master FORCE-UPDATED to **v0.4.3 @ 11dccfad5** — 2 commits, both fixing open agent vi issues: **#2437 (`cw`) and #2439 (`i"`/`a"` forward search) CONFIRMED FIXED via UI, commented on both**. During the #2439 recheck found **#2604**: `a"` excludes trailing whitespace unlike Vim `:help aquote` — `da"` on `the "quick" brown fox` leaves `the  brown fox` (double space), from col 1 AND inside quotes. LSP diagnostics deep-dive + signature help + code actions + auto-import via rust-analyzer 1.94.1 Full mode: hover fusion/severity colors/F8 wrap/panel goto+preview+`a`-filter/inline text/live clears-on-fix + signature-help layout incl. active-param advance + BOTH code-action families (Fill struct fields quickfix; Generate `new` assist WorkspaceEdit end-to-end) ALL PASS. Filed **#2601**: dismissing a hover popup (Esc or cursor-move) that doesn't overlap an error WIPES that error from gutter/status-count/F8 until next save (panel still lists it; hover-on-the-error preserves; 5/5). Filed **#2602**: diagnostic positions frozen at publish — lines inserted above (incl. editor-applied code actions) leave ● and F8 pointing into wrong/generated code until re-save (stale-position family of #2583, diagnostics subsystem). Filed **#2603**: completions NEVER include unimported symbols (bare `HashMap`: no popup at all; member/prelude/qualified controls complete fine) → lsp.md's "Auto-imports are applied when you accept a completion" unreachable. slangd still absent.)
Prev: Run #55, 2026-07-07 (v0.4.3 @ 4e945b494 unchanged since Run #50 — no rechecks due, no maintainer action on the 22 open agent issues. Rust LSP request-based deep dive via rust-analyzer (the #2197-blocked family, first-ever request coverage): hover/F12 same+cross-file/Shift+F12 references (6 refs incl. per-occurrence rows + Enter-nav)/Ctrl+F12 implementations (trait name → 2 impl blocks; method call → 2 impl fns)/F2 rename (cross-file 4-change, undo-able)/Ctrl+Space completions (correct member list, Tab-accept, docs-confirmed manual-only trigger) ALL PASS in Full mode. Filed **#2598**: Reduced Memory mode silently kills hover/Go-to-Def on macro names and anywhere inside macro invocations (println!/format! args) — permanent (5min warm), non-macro positions + Full mode work; status claims only checkOnSave/procMacro/cachePriming disabled; also notes the 30–60s "ready"-but-empty warm-up window after reduced restarts + the lsp.md doc-name mismatch ("Switch Rust Analyzer Mode" vs actual "Rust LSP: Configure Mode"). Filed **#2599**: cross-file F2 rename from a use site moves focus to the definition's file at that buffer's STALE cursor position (2/2; def-site + single-file controls stay put; VS Code keeps focus). slangd still absent → Slang Go-to-Def skipped again.)
Prev: Run #54, 2026-07-07 (v0.4.3 @ 4e945b494 unchanged since Run #50 — no rechecks due. #2540 large-file 10MB single-threshold rework: core contract PASS (2MB/8MB eager with exact line jumps; 12MB/150MB lazy byte-offset mode; `large_file_threshold_bytes` honored in both directions; Ctrl+G scan offer, decline→byte-offset fallback, "Scan Line Index" palette cmd). Filed **#2596**: the FIRST line-index scan undercounts the total (max 299828 from-top 3/3 / 298189 from-EOF 2/2 vs true 300001) → Go to Line clamps below the real end, EOF gutter non-monotonic, status vs gutter disagree on one frame (`Ln 150001` vs `298189`); re-running "Scan Line Index" corrects (workaround); 150MB first scan correct. Filed **#2597**: palette `:N` line mode in an UNSCANNED large file jumps to EOF and reports "Jumped to line 1" — no scan offer (Ctrl+G in the same buffer offers it properly). #2190 inlay hints wrap/h-scroll: COMPREHENSIVE PASS via rust-analyzer — hint widths participate in wrap decisions (41-char line wraps at 60 cols), no dropped text at wrap boundaries, h-scroll shows true EOL + cursor, Ln/Col stay real-column. slangd still absent → Slang Go-to-Def skipped again.)
Prev: Run #52, 2026-07-07 (v0.4.3 @ 4e945b494 unchanged since Run #50 — no rechecks due. Filed **#2594**: the documented theme color-transition animation never plays — all five switch paths (picker preview/apply/cancel, Settings dropdown+Ctrl+S, plugin `applyTheme`) swap colors instantly with zero interpolated frames over 1s+ millisecond bursts, while the tab-switch slide from the SAME animations framework DOES animate (control). GDScript #2238 comprehensive PASS (filetype/coloring/auto-indent/refs; Godot LSP disabled-by-default = `LSP (off)`). Dotfiles reveal #2407 PASS (`.`-prefix filter + `Alt+.` toggle). No R3-worthy nits.)
Prev: Run #51, 2026-07-07 (v0.4.3 @ 4e945b494 unchanged since Run #50 — no rechecks due. Tested the monorepo/multi-repo workspace series `3a78ad5ec`…`0bfb54edf`: blame `b`-history/find-file relative paths/Review Diff scoping/sibling-prefix decorations/levels-1..3 discovery ALL PASS. Filed **#2591**: standalone Git Grep still greps the WORKSPACE ROOT (multi-repo root → exit 128 masked as "No matches" + ⚠ badge; nested sub-repo buffer → silently searches the outer repo). Filed **#2592**: explorer git decorations ignore a nested sub-repo's status when the root is itself a repo (inner.py modified per gutter/blame/Review Diff but undecorated in the tree). Blank `?` row under untracked nested-repo dir in Review Diff → IMP-027, noted in #2592, not filed.)
Prev: Run #50 (v0.4.3 @ 4e945b494 — master gained the monorepo/multi-repo git workspace series + `4e945b494` fixing agent issue **#2442** (vi j/k clamp — CONFIRMED FIXED via UI, commented + resolved). Filed **#2587**: File Explorer right-click context menu doesn't grab the keyboard — printable keys/Backspace fall through to the tree's type-ahead (`/x` header), move the selection BEHIND the open menu, and menu actions follow the moved selection (proven: right-click alpha.txt, type `sub`, Copy Relative Path → `subdir`). Shader grammar smoke GLSL/HLSL/WGSL/Slang all PASS.)
Prev: Run #49 (v0.4.3 @ 89d91e84d — master gained only a GLSL/HLSL/WGSL grammar commit. NO new issue filed: mouse-driven UI sweep via the SGR harness was a comprehensive PASS — '+' popup + tab context menu incl. 0.4.2 keyboard-grab claims, drag/double-click/wheel, terminal Ctrl+Click + OSC 7, theme popup, split terminals. Editor right-click no-op + unreachable `menu.terminal.*` strings → IMP-025, not filed per R3.)
Prev: Run #48 (v0.4.3 @ 9f6135001 unchanged. Filed **#2583**: S&R match stepping `Ctrl+Alt+→/←` + Enter-open land on stale pre-edit positions after the buffer is edited (positions recorded at search time, never adjusted; workaround = re-run search). Stepping otherwise a comprehensive PASS; 0.4.2 indentation guides comprehensive PASS incl. #2535/#2564-shape regression spot-checks holding.)
Prev: Run #47 (v0.4.3 @ 9f6135001 unchanged. Filed **#2582**: typing a dedent trigger (Python `else:`, custom `decrease_indent_pattern` token) does not re-indent the line — dedent applies only at Enter-time; `}` alone dedents live. Resolves Run #46's murky `decrease_indent_pattern` characterization; references closed #2314 (its "type CLOSE on an indented line" expected-table row is the still-broken case; `dedent_next_line_pattern` verified WORKING). Virtual space block mode / persistence / click-past-EOL all PASS, no new bug.)

---

## Open Issues (agent-filed)

| [#3136](https://github.com/sinelaw/fresh/issues/3136) | Terminal input replay: the command typed before opening the Run Agent dialog is re-injected into the shell as pending input after switching workspaces away and back | Run #61 | **Open** | v0.4.10 @ 7bcc8ff6c (retained-mode series). Recipe 3/3: run `echo X` in a workspace terminal → Run Agent… dialog → Esc → dock switch away+back → `echo X` re-appears at the prompt as PENDING PTY input; **Enter re-executes it** (proven real input, not render). One-shot per dialog-open; bounce-only + palette-only controls clean (0/2). Body also notes the 1× palette-query-into-dialog-field leak (same family). 0.4.9 changelog explicitly requests input-anomaly reports. Med-high (silent command re-execution risk). Workaround: C-u the pending line. Searches (0 dupes): `terminal input replayed command re-appears pending after workspace switch` / `keystrokes injected duplicated typed text terminal again orchestrator` / `Run Agent dialog input leak repeat shell command executed twice`; related-distinct closed: #2745/#2246/#2546. Do NOT re-file. |
| [#3137](https://github.com/sinelaw/fresh/issues/3137) | Orchestrator "Move to Folder…" popup does not own the keyboard: Down/Up fall through to the dock, switch the live workspace, and dismiss the popup | Run #61 | **Open** | v0.4.10 @ 7bcc8ff6c. Popup footer promises `↑↓ choose Enter select Esc cancel`; actual ↓ moves the DOCK selection behind it (live-switches the visible workspace!) and the popup dismisses — 3/3, ANSI-verified (48;5;25 highlight on dock row; popup rows never highlight). Keyboard-only folder filing impossible. REGRESSION: closed #2694 (PR #2696) listed this dropdown among those supporting ↑↓; retained-mode keyboard-layer rework suspect. Controls OK: F2 folder menu + New Folder dialog take keys fine. Workaround: mouse-click the popup rows. Searches (0 dupes): `Move to Folder popup arrow keys switch workspace instead of choosing` / `orchestrator dock folder popup keyboard focus dismissed down arrow` / `dock dropdown keyboard navigation regression`. Do NOT re-file. |
| [#3074](https://github.com/sinelaw/fresh/issues/3074) | Terminal tab name not preserved across editor restart (restored exited terminal shows generic `*Terminal N*` instead of `bash — <cwd>` auto-title) | Run #60 | **Open** | v0.4.10 @ 992e61d02. Both terminal.md §Persistence ("tab names are preserved across editor restarts") and the 0.4.6 changelog ("terminal tab names now survive an editor restart", ex `claude (exited)`) promise the name survives. Actual: a terminal EXITED before quit comes back after `fresh` pure-restore with a generic `*Terminal N*` name; `(exited)` marker + `⟳ Restart` indicator + scrollback all restore fine, only the name is lost. 2-terminal repro: `bash — .../subdir (exited)` + `bash — ... (exited)` → both become `*Terminal 0* (exited) 1` / `*Terminal 0* (exited) 2` (cwd distinction unrecoverable from tab bar). Cause narrowed: name re-derived from the live process each time (restart regains `bash — ...`; dead restored terminal has no live process → internal buffer name). Med usability. Searches (0 dupes): `terminal tab name restart restore` / `terminal tab generic name exited restore session` / `terminal name survive editor restart`; related-but-distinct #2828(closed)/#2485(closed)/#2282(feature req). Do NOT re-file. |
| [#2735](https://github.com/sinelaw/fresh/issues/2735) | Extract Tab to New Workspace: the new co-tenant workspace is not persisted, so it and its tab vanish on restart | Run #59 | **FIXED** (Run #60) | CONFIRMED FIXED in v0.4.10 (fix landed 0.4.9 per changelog "Workspaces made with Extract Tab to New Workspace survive a restart, and --no-restore truly writes no session state (#2735)"), already maintainer-closed → recorded, not re-commented. UI recheck: `fresh main.rs lib.rs` → Extract Tab on lib.rs → 2 co-tenant windows → Quit → **TWO** `workspaces/<root>.ws-<stable_id>-N.json` (was ONE at file time) → relaunch `fresh` → BOTH windows restore (dock shows both; `wr60`=main.rs, `wr60 (2)`=lib.rs). Second half: `--no-restore` writes no session layout (only trust.json); pure restore after = empty `[No Name]`. Do NOT re-file. Orig (Run #59): NEW 0.4.4 feature (co-tenant workspaces + commit "Restore each co-tenant window to its own file on reboot"). Clean repro 2/2: `fresh --no-restore main.rs lib.rs` → palette **Extract Tab to New Workspace** on lib.rs → two co-tenant windows over the same root (verified via Orchestrator modal + **Alt+I** "show 1-file workspaces"; both are 1-file) → Quit → only ONE `workspaces/<root>.ws-<stable_id>-N.json` on disk (the SOURCE, main.rs); the extracted lib.rs co-tenant is never written → restart restores only main.rs (Alt+I confirms no 2nd). Source (also 1-file) survives → not the Alt+I filter. Med-high (silent loss of the workspace arrangement the feature creates; unsaved extracted buffers would be data loss). Workaround: none across restart. Do NOT re-file. |
| [#2606](https://github.com/sinelaw/fresh/issues/2606) | vi mode: visual-block (Ctrl+V) indent operators `>` / `<` do nothing and don't exit VISUAL BLOCK mode | Run #58 | **FIXED** (Run #59) | CONFIRMED FIXED in v0.4.4 via UI (maintainer closed 2026-07-09; CHANGELOG #2606): Ctrl+V + jj over lines 1–3, `>` indents all 3 by 4 spaces AND returns `-- NORMAL --`; `<` dedents back + NORMAL. Residual gap of the #2438 family (fix `cbe2b412c` covered NORMAL `>>`/`<<` + VISUAL/VISUAL LINE `>`/`<` — all verified working). Ctrl+V block + `j` + `>` → buffer unchanged, no feedback, mode STAYS `-- VISUAL BLOCK --` (2/2; `<` 1/1). Controls: `V`+`>` indents + returns NORMAL; block `x` deletes the rect + NORMAL → blockwise indent ops specifically unhandled. Vim ref `:help v_b_>` (shift lines right, return to NORMAL). References owner tracking #2447. Workaround: use `V` visual-line + `>`/`<`. Med. Do NOT re-file. |
| [#2605](https://github.com/sinelaw/fresh/issues/2605) | Format Buffer ignores the active selection and reformats the whole file — documented LSP range formatting unreachable when a default formatter exists | Run #57 | **Open** | First-ever Format Buffer test (v0.4.3 @ 6ab255709, rust-analyzer 1.94.1, Trusted). Selection lines 1–4 (ANSI-verified) + Format Buffer → line 6 OUTSIDE the selection also reformatted (`a+b`→`a + b`), status `Formatted with rustfmt`, no scope warning. LSP fallback (docs lsp.md: "falling back to LSP formatting (including range formatting) when none is set") unreachable for Rust: default rustfmt runs even with LSP STOPPED; `"formatter": null` doesn't unset (field-merge); `""`/`/nonexistent` → `Format failed: Formatter 'X' not found`, NO fallback with server ready. Whole-buffer path itself is solid (byte-identical to rustfmt, single undoable edit, disk untouched until save; custom `{command,args}` works — file path auto-appended). Med. Workaround: none for range. Do NOT re-file. |
| [#2604](https://github.com/sinelaw/fresh/issues/2604) | vi mode: `a"` text object does not include the trailing whitespace, unlike Vim — `da"` leaves a double space | Run #56 | **FIXED** (Run #59) | CONFIRMED FIXED in v0.4.4 via UI (maintainer closed 2026-07-09; CHANGELOG #2604): `da"` from col 1 on `the "quick" brown fox` → `the brown fox$` (single space, cat-A). Found verifying the #2439 fix (v0.4.3 @ 11dccfad5). Vim `:help aquote`: `a"` includes the quotes AND trailing whitespace (or leading if no trailing). Fresh's `da"` on `the "quick" brown fox` deletes only `"quick"` → `the  brown fox` (double space, cat -A-verified), from col 1 and from inside the quotes (2/2). `di"` unaffected. The forward-search rule itself (the #2439 fix) works. Med-low. Workaround: `x` the leftover space. Do NOT re-file. |
| [#2603](https://github.com/sinelaw/fresh/issues/2603) | LSP completions never offer unimported symbols, so the documented "auto-imports are applied when you accept a completion" can never trigger | Run #56 | **Open** | Found testing lsp.md's auto-import claim (v0.4.3 @ 11dccfad5, rust-analyzer 1.94.1 Full mode, checkmarked ready, no `use` stmts). Bare `HashMa`/`HashMap` + Ctrl+Space (and palette Show Completions) → NO popup at all, ever; trait-method needing import (`p.type_i`) → nothing. CONTROLS same session: `p.` members ✓, `Strin` → String/stringify! (prelude) ✓, `std::collections::HashMa` → HashMap (qualified) ✓. Every candidate requiring an auto-import is missing (no flyimport). VS Code + same RA offers `HashMap (use std::collections::HashMap)` and inserts the use on accept. Med. Workaround: type the qualified path / write use manually. Do NOT re-file. |
| [#2602](https://github.com/sinelaw/fresh/issues/2602) | LSP diagnostics don't track buffer edits: gutter markers and F8 point at stale lines after lines are inserted above — even for editor-applied code actions | Run #56 | **Open** | Found in the diagnostics deep-dive (v0.4.3 @ 11dccfad5, rust-analyzer). Error published at line 8 (`p.zzz`); accepting "Generate `new`" (Alt+. assist) inserts 6 lines above → ● stays on OLD line 8 (now inside the freshly GENERATED fn), F8 jumps to 8:17 mid-generated-code with the zzz message, real error line (14) unmarked; panel lists the stale position too. Corrects on next save (re-check). VS Code shifts diagnostic ranges with edits — especially expected for editor-applied WorkspaceEdits where the deltas are known. Same stale-position family as #2583 (S&R), different subsystem. Med. Workaround: save to re-check. Do NOT re-file. |
| [#2601](https://github.com/sinelaw/fresh/issues/2601) | LSP diagnostics: dismissing a hover popup removes error diagnostics elsewhere from gutter, status bar, and F8 until the next save | Run #56 | **Open** | Found in the diagnostics deep-dive (v0.4.3 @ 11dccfad5, rust-analyzer, error `p.zzz` + warnings from checkOnSave). Hover (Alt+K) at any position NOT overlapping the error (the warning line, or a clean call site), then dismiss via Escape OR cursor-move → the ERROR vanishes from the status severity counter (E:1 W:2 I:1 → W:2 I:1), the gutter (● gone), and F8 (cycles only the warnings; error position unreachable) — while the Diagnostics panel STILL lists it and the code is still broken. 5/5. Controls: hover ON the error + dismiss preserves (2/2); bare Escape preserves; panel open/close (q or Esc) preserves. Warnings/hints never drop. Re-save restores every time. Med — hover is constant-use; every hover away from an error silently hides it. Do NOT re-file. |
| [#2599](https://github.com/sinelaw/fresh/issues/2599) | Rename Symbol (F2): cross-file rename moves focus to the definition's file at a stale cursor position instead of staying in the invoking file | Run #55 | **Open** | Found in the Rust LSP request deep dive (v0.4.3 @ 4e945b494, rust-analyzer). F2 on a field's USE site in main.rs (def in shapes.rs): rename applies correctly ("Renamed successfully (4 changes)", both buffers dirty) but the ACTIVE TAB switches to shapes.rs, cursor at that buffer's stale previous position (line 7 while the renamed def was line 11 — not even the symbol). 2/2 (radius, side). Controls: rename invoked AT the def site stays put; single-file rename from a call site (describe, 3 changes) stays put → cross-file WorkspaceEdit application steals focus. Reference: VS Code/IntelliJ keep focus in the invoking file. Med-low. Workaround: "Switch to Previous Tab". Do NOT re-file. |
| [#2598](https://github.com/sinelaw/fresh/issues/2598) | Rust LSP Reduced Memory mode: hover and Go to Definition silently fail on macro names and anywhere inside macro invocations (println!/format! args) | Run #55 | **Open** | Found testing "Rust LSP: Configure Mode" (v0.4.3 @ 4e945b494, rust-analyzer 1.94.1, tiny trait crate). In Reduced Memory mode (warmed 60s–5min): F12/Alt+K on `describe` inside `println!` args → "No definition found"/"No hover information available" (multiple lines, permanent); hover on `println` itself dead; F12 on `name` inside `format!` args dead. Non-macro controls SECONDS apart all work: bare `area_sum` call, `Circle` struct literal (cross-file), fn-def hover, `self`. Full mode: identical positions work, and recover within seconds of switching back. Status message claims only "checkOnSave, procMacro, cachePriming disabled" (println! is a builtin macro — none of those explain it); no warning surfaced. Secondary: after every reduced-mode restart ALL requests return empty for 30–60s while status says "LSP (rust) ready" (Full mode answers in seconds). Also noted in body: lsp.md documents the command as "Switch Rust Analyzer Mode" but the palette name is "Rust LSP: Configure Mode". Med. Workaround: switch back to Full Mode. Do NOT re-file. |
| [#2597](https://github.com/sinelaw/fresh/issues/2597) | Large files: palette ":N" line jump before a line-index scan jumps to end of file and reports "Jumped to line 1" | Run #54 | **Open** | Found verifying the #2540 threshold rework (v0.4.3 @ 4e945b494). In an unscanned >10MB buffer (byte-offset gutter), `Ctrl+P`+`C-u`+`:1000`+Enter → cursor lands at `Byte 12000000` (file END) with status "Jumped to line 1" (2/2, also `:150000`). No scan offer, wrong destination, wrong message. Controls: `:N` exact in 2MB/8MB files; `Ctrl+G` in the SAME buffer correctly offers "Scan file for exact line numbers?" and `n` falls back to a byte-offset prompt; post-scan `:N` works. Med. Workaround: use Ctrl+G / palette "Go to Line". Do NOT re-file. |
| [#2596](https://github.com/sinelaw/fresh/issues/2596) | Large files: first line-index scan undercounts total lines — Go to Line clamps below the real last line, EOF labeled with wrong line number | Run #54 | **Open** | Found verifying the #2540 threshold rework (v0.4.3 @ 4e945b494). 12MB fixture of 300,000 40-byte lines (true last line 300001): first scan yields max **299828** (viewport-at-top, 3/3 at 1s/3s/15s — not a load race) or **298189** (cursor-at-EOF, 2/2); jumps ≥ max clamp (299500/999999 both) so real lines above it are unreachable by line jump; EOF gutter row non-monotonic (`298189` under a correct `300000`); after an at-EOF scan status said `Ln 150001` while gutter said `298189` for the same position. Below-max mappings exact (150000/298188 content-verified). **Re-running "Scan Line Index" corrects to 300001 (2/2)** = workaround. 150MB fixture scans correctly first time. Med. Do NOT re-file. |
| [#2594](https://github.com/sinelaw/fresh/issues/2594) | Theme-switch color-transition animation never plays: every switch path swaps colors instantly despite animations being enabled | Run #52 | **Open** | Found testing the long-standing untested "theme color-transition animation" backlog item (v0.4.3 @ 4e945b494). Docs (`docs/configuration/index.md` §Screensaver + 0.4.0 blog) promise "Switching themes plays a brief color-transition animation" (toggleable). Millisecond frame-burst (`capture-pane -e -p` ~4ms/frame, 200–300 frames) shows ALL FIVE switch paths swap old→final palette in adjacent frames (4ms) with ZERO interpolated frames over 1s+: Select Theme picker live-preview (arrows), apply (Enter), cancel (Esc); Settings UI Theme dropdown + Ctrl+S; plugin `editor.applyTheme()` from init.ts. CONTROL: the tab-switch slide from the SAME `editor.animations` framework DOES render transient frames (~16-col offset ~170ms) — so animations run + truecolor works; the transition specifically never fires (regressed/removed, or docs stale). Cosmetic, no workaround. Do NOT re-file. |
| [#2592](https://github.com/sinelaw/fresh/issues/2592) | File Explorer git decorations ignore a nested sub-repo's status when the workspace root is itself a git repo | Run #51 | **Open** | Found testing the monorepo series (v0.4.3 @ 4e945b494). Root repo + nested `vendored/` repo w/ uncommitted inner change: `inner.py` gets NO decoration and `vendored` shows only outer's `U`, while Live Diff gutter, Git Blame, and Review Diff on the SAME file all resolve the inner repo (Review Diff scopes to `M inner.py +1/-0`). Contrast: when the root is NOT a repo, sub-repo files decorate correctly (same build, `M` on `app/main.py`). Med. Body also notes the IMP-027 blank `?` row (#2315 shape) as a related observation. Do NOT re-file. |
| [#2591](https://github.com/sinelaw/fresh/issues/2591) | Git Grep always runs in the workspace root: hard failure in multi-repo workspaces, wrong repo searched from nested sub-repo buffers | Run #51 | **FIXED** (Run #59) | CONFIRMED FIXED in v0.4.4 via UI (maintainer closed 2026-07-15; CHANGELOG #2591 + repo-scoping via #2369): "Git Grep" for a non-existent string → status "No matches", NO `[⚠]` badge (was exit-128 ERROR). Found testing the monorepo series (v0.4.3 @ 4e945b494). Standalone "Git Grep" palette cmd not updated by the series: root-not-a-repo → every search "No matches" + `[⚠]` ERROR (`exit 128 fatal: not a git repository`, 3/3 across two sub-repos); root-is-a-repo + nested sub-repo buffer → silently greps the OUTER repo. Papercut folded in: exit 1 (normal no-match) also logged as ERROR w/ badge. Contrast verified same build: git_find_file (repo-relative paths ✓), live_grep git-grep provider ✓, git_blame ✓, audit_mode ✓ all resolve the buffer's repo. Workaround: Live Grep w/ git-grep provider. Med. Do NOT re-file. |
| [#2587](https://github.com/sinelaw/fresh/issues/2587) | File Explorer context menu does not grab the keyboard: keys fall through to the tree's type-ahead and can silently retarget menu actions | Run #50 | **Open** | Found on first-ever test of the explorer right-click menu (v0.4.3 @ 4e945b494, SGR mouse harness). Menu = New File/New Directory/Rename/Cut/Copy/Paste/Delete/Duplicate/Copy Full Path/Copy Relative Path; arrows/Enter/Esc go to the menu, but printable keys + Backspace fall through to the explorer type-ahead (`/x` in sidebar header), which MOVES the selection behind the open menu — and menu actions act on the live selection, not the right-clicked row (right-click alpha.txt → type `sub` → Copy Relative Path copied `subdir`; 3/3 leaks with `q`/`z`/`sub`). Leaked filter also persists after menu closes. Contrast: tab context menu + '+' popup DO grab keys (0.4.2 changelog claim, re-verified Run #49). Med. Workaround: don't type while menu open; Esc in explorer clears stray filter. Do NOT re-file. |
| [#2583](https://github.com/sinelaw/fresh/issues/2583) | Search & Replace: match stepping (Ctrl+Alt+→/←) and Enter-open land on stale pre-edit positions after the buffer is edited | Run #48 | **Open** | Found testing the 0.4.2 stepping feature (#2434) in v0.4.3 @ 9f6135001. Panel match positions are recorded at search time and never adjusted for buffer edits: insert a line above a match (or `xx` before it on its line) → every later `Ctrl+Alt+→/←` step and Enter-open in that buffer lands the REAL cursor (tmux `#{cursor_x}/#{cursor_y}`-verified) on the old line/col over unrelated text; panel previews stay stale too. Unedited files keep landing correctly. Everything else about stepping PASSES (fwd/back, wrap both ways, cross-file source-split open, editor focus for immediate edit, panel highlight follows, palette cmds, graceful no-search msg). Med. Workaround: re-run the search (`Alt+A`, Enter). Do NOT re-file. |
| [#2582](https://github.com/sinelaw/fresh/issues/2582) | Typing a dedent trigger does not re-indent the line: Python `else:` and custom `decrease_indent_pattern` tokens stay over-indented (only `}` dedents live) | Run #47 | **Open** | Focused follow-up to closed #2314 (that issue's own expected table listed "type `CLOSE` on an indented line → dedents 1 level"; still broken in v0.4.3 @ 9f6135001). Dedent rules apply ONLY at Enter-time (Enter-split before `CLOSE tail` correctly dedents the moved-down line). Typing the trigger never re-indents: built-in Python `if a:`/`x = 1`/`else:` → `else:` stays at indent 4 (VS Code+Sublime+Vim dedent live) and the following line then compounds to 8; custom `decrease_indent_pattern:"^\\s*CLOSE\\b"` same. CONTROL: C `}` on indent-4 line DOES live-dedent to col 1 (electric bracket) — keyword triggers should match. Doc's own `begin/end` example unusable in practice (you type `end`, never Enter-split before it). `increase_indent_pattern` + `dedent_next_line_pattern` verified WORKING. Med usability. Workaround: manual Shift+Tab. Do NOT re-file. |
| [#2577](https://github.com/sinelaw/fresh/issues/2577) | Virtual space: status-bar Ln/Col indicator freezes at the line-end position while the cursor is in virtual space | Run #46 | **FIXED** (Run #59) | CONFIRMED FIXED in v0.4.4 via UI (maintainer closed 2026-07-12; CHANGELOG #2577): with Virtual Space on, End→Col6 on `short` then Right×4 → status `Ln1 Col10` (tracks the cursor 4 past EOL, no longer frozen); typing at Col10 materialized `short    ZZ`. Found testing the new Virtual space feature (0.4.3 headline, master @ 9f6135001). With `editor.virtual_space` on, moving the cursor past EOL (horizontal) freezes the `Col` readout at the EOL column while the real cursor advances (proven via `tmux #{cursor_x}` + typing a marker that lands at the true column); vertical virtual space (ArrowDown below last line) freezes BOTH `Ln` and `Col`. Readout only corrects when the cursor returns to real text. Reference = Visual Studio (feature's stated model) reports the virtual column. Distinct trigger/symptom from #2301. Low–med usability; the position indicator is wrong for the whole duration in the mode built for free positioning. Workaround: type to materialize, or move back to real text. Do NOT re-file. |
| [#2443](https://github.com/sinelaw/fresh/issues/2443) | vi mode: dot-repeat (`.`) of `o`/`O`/`a`/`A` corrupts the buffer (injects unrelated line content) instead of replaying the typed text | Run #45 | **Open** | Found finishing the vi-command sweep (master @ 1c6bd8ce9, v0.4.1). `o`+`abc`+`Esc` (line2=`abc`, correct) then `.` → line2=`abhello world foo bar baz`, line3=`abcc` (line 1's content injected; expected line3=`abc`). Also `A`+`X`→`shortX` then `.`→`shortshortXX`; `a`+`Y`→`sYhort` then `.`→`ssYYhort`. CONTROL works: `i`+`AB`→`ABalpha` then `.`→`AABBalpha` (correct); `x` dot-repeat correct. Defect specific to cursor-repositioning insert cmds. Med-sev: silent corruption from common `o`/`a`+`.` workflow. Workaround: avoid `.` after `o`/`O`/`a`/`A`. Do NOT re-file. |
| [#2442](https://github.com/sinelaw/fresh/issues/2442) | vi mode: `j`/`k` onto a shorter line park the cursor one past the last char, so `x` deletes the newline and joins lines | Run #44 | **FIXED** (Run #50) | Fix commit `4e945b494` "fix(vi): clamp j/k onto a shorter line to its last character". CONFIRMED FIXED in v0.4.3 @ 4e945b494 via UI: all 3 original paths pass — `$`+`j` onto `short` → Col 5, `x` → `shor` (no join); col-10 `j` clamps AND goal column restores (Col 10 on the next long line, Vim desired-column); `k` from below → Col 5, `x` → `shor`. Comment added. Do NOT re-file. Orig: Found continuing the vi-compat sweep (master @ 3b8c2eca1, v0.4.1). `$` on a long line (col correct) then `j` onto `short` (5 chars) → status `Ln, Col 6` (=len+1, past EOL); `x` deletes the newline → `shorta longer line...` (lines joined). 3/3 via `$`+j, `l`-to-col10 + j, and `k` from below. HORIZONTAL clamps correctly (`$` direct on `short`→col5→`x`=`shor`; `l` stops at last char) → vertical column-clamp off-by-one. Med-sev: silent corruption from navigate-then-`x`. Workaround: press `$`/`0` to re-anchor before `x`. Do NOT re-file. |
| [#2441](https://github.com/sinelaw/fresh/issues/2441) | vi mode: find-char motions don't work with operators (`df`/`dt`/`cf`/`ct` hang or mis-delete) and `;`/`,` repeat is a no-op | Run #44 | **FIXED** (Run #57) | Closed by maintainer 2026-07-07, fix commit `f242ec149`. CONFIRMED FIXED in v0.4.3 @ 6ab255709 via UI: `dfr` → `ld foo bar baz` (no hang, inclusive); `dfw` → `orld foo bar baz` (motion-key target no longer dropped); `fo`+`;` → col 8 (x-verified), `,` → back (x-verified); `ctb`+X → `Xbar baz` + INSERT; backward `dFo` → `hello world foz`, `dTo` → `hello world fooz`. Comment added. Do NOT re-file. Orig: | Found continuing the vi-compat sweep (master @ 3b8c2eca1, v0.4.1). Pure `f`/`t` cursor motions WORK. But `d`/`c`+`f`/`t` HANGS in `-- OPERATOR (d/c) --` (target + even `Z` swallowed; Esc to recover; held at 0.4s AND 1.2s gaps → not timing). When target is itself a motion key, `f` is dropped: `dfw`→`dw` (`world foo bar baz`, not `orld...`). `;`/`,` repeat = no-op (proved via `fo`+`;`+`x` deleting col5 not col8). Med-sev: extremely common ops. Workaround: non-find motions / `/search`. Do NOT re-file. |
| [#2438](https://github.com/sinelaw/fresh/issues/2438) | vi mode: indent operators `>>`/`<<` (and visual-mode `>`/`<`) do nothing | Run #43 | **FIXED** (Run #57) | Closed by maintainer 2026-07-07, fix commit `cbe2b412c`. CONFIRMED FIXED in v0.4.3 @ 6ab255709 via UI: `>>` indents one level (4sp) + cursor to first non-blank; `<<` dedents to col 1; `V`(+`j`) then `>`/`<` indents/dedents the selected lines AND returns to NORMAL. Comment added. Do NOT re-file. Orig: | Found continuing the vi-compat motion sweep (master @ 8ee2baf31, v0.4.1). `>>` in NORMAL = no-op (line unchanged, status stays `-- NORMAL --`); `<<` on a 4-space-indented line keeps the indent (cursor confirmed on the line); visual `V` then `>` leaves the line unchanged AND stays `-- VISUAL LINE --` (Vim would indent + drop to NORMAL). `>` key transmits fine (literal insert verified) → indent operator family unhandled in vi mode, no feedback. Med-sev: core Vim editing command silently does nothing. Workaround: leave NORMAL, use editor `Tab` indent. Do NOT re-file. |
| [#2439](https://github.com/sinelaw/fresh/issues/2439) | vi mode: quote text-objects `i"`/`a"` don't search forward on the line (only work when cursor already inside the quotes) | Run #43 | **FIXED** (Run #56) | Closed by fix commit `11dccfad5`. CONFIRMED FIXED in v0.4.3 via UI: `di"` from col 1 → `the "" brown fox`; `ci"` from col 1 → INSERT + `the "WXYZ" brown fox`; `da"` forward-searches too. Comment added. Remaining nuance filed separately → #2604 (`a"` excludes trailing whitespace). Do NOT re-file. Orig: | Found continuing the vi-compat motion sweep (master @ 8ee2baf31, v0.4.1). `di"`/`ci"` from column 1 of `the "quick" brown fox` (before the quote, same line) = silent NO-OP (2/2). Vim's `i"`/`a"` search forward on the current line, so `ci"` from a line start (common case) should hit `"quick"`. WORKS only with cursor inside the quotes: on `q`, `di"` → `the "" brown fox`, `ci"` → INSERT `the "WXYZ" brown fox`. `"` key transmits fine. Med-sev. Workaround: move inside quotes first (`f"`/`t"`). Do NOT re-file. |
| [#2437](https://github.com/sinelaw/fresh/issues/2437) | vi mode: `cw` deletes the trailing whitespace instead of acting like `ce`, unlike Vim | Run #42 | **FIXED** (Run #56) | Closed by fix commit `463c435e3`. CONFIRMED FIXED in v0.4.3 via UI: `cw`+`X` at col 1 → `X world foo bar` (space kept); mid-word `heX world foo bar`; `dw` control still eats the space correctly. Comment added. Do NOT re-file. Orig: | Found testing the new vi "Vim compatibility options" / "align vi compatibility motions" commits (master @ eb3a349e6, v0.4.1). In Vim, `cw` on a non-blank acts like `ce` (changes to end of word, KEEPS the trailing space) — `:help cw`. Fresh's `cw` instead behaves like `dw`: on `hello world foo bar`, `cw`+`X` → `Xworld foo bar` (space eaten) while `ce`+`X` → `X world foo bar` (correct). Reproduced 2/2 at word start AND mid-word (`heXworld`). `dw` correctly keeps space-eating behavior (control). Med-sev: a Vim user hits this constantly. Workaround: use `ce`. Do NOT re-file. |
| [#2420](https://github.com/sinelaw/fresh/issues/2420) | Review Diff: line-level discard shows raw i18n key `status.lines_discardd` instead of a localized message (key is misspelled) | Run #38 | **FIXED** (Run #46) | Closed by maintainer (0.4.2). CONFIRMED FIXED in v0.4.3 via UI: line-level discard (`v` on `-`, `j`, `d`) now shows localized `Lines discarded` (git-verified revert). Do NOT re-file. Orig: After the #2317 fix, line-level **discard** works but its status-bar success message prints the literal lookup key `status.lines_discardd` (typo "discardd", double-d — likely why the translation lookup misses). CONTRAST: line-level **stage** shows proper `Lines staged`. Deterministic across 2+ discards (modification pair `v`,`j`,`d`). Low-sev cosmetic; discard itself git-verified correct. Do NOT re-file. |

| [#2317](https://github.com/sinelaw/fresh/issues/2317) | Review Diff: line-level visual staging/unstaging/discard (`v` then `s`/`u`/`d`) never works | Run #36 | **FIXED** (Run #38) | Closed by maintainer 2026-06-22 (fix commit `a1d3e4352` "resolve line-level stage/unstage/discard hunk by id"). CONFIRMED FIXED in v0.4.1 via UI: `v`+`s` stages a single line (git diff --cached shows only it); `v`+`u` unstages a single staged line; `v`+`d` discards a clean pure-addition, and a modification when the full `-`/`+` pair is selected (`v`,`j`,`d`). All git-verified, cursor ANSI-verified each op. Comment added. (Edge: `v`+`d` on the lone `+` of a modification → "patch does not apply" — likely expected; logged as potential_improvement, not re-filed.) Do NOT re-file. |
| [#2318](https://github.com/sinelaw/fresh/issues/2318) | Review Diff: file-level Discard (`D`) reports "Discarded" but leaves staged changes intact for a fully-staged file | Run #36 | **Open** | `D` "Discard changes in file" only reverts the WORKING tree. On a fully-staged file (`M `, no working diff) it reports `Discarded: <file>` after a "this cannot be undone" dialog but git status / `git diff --cached` / disk are unchanged — nothing discarded. Contrast: on an UNSTAGED file `D` correctly reverts to HEAD. `D` only ever touches the index-free working tree; UI claims a full file discard regardless. (Also: `D`/`d`/`x` only fire when cursor is on a hunk CONTENT row.) Workaround: unstage first, or `git restore --staged --worktree`. Do NOT re-file. |
| [#2315](https://github.com/sinelaw/fresh/issues/2315) | Review Diff: untracked directories are not expanded — files in a new folder show as a blank, contentless entry and can't be reviewed | Run #35 | **FIXED** (Run #46) | Closed by maintainer (0.4.2). CONFIRMED FIXED in v0.4.3 via UI: untracked `assets/` dir now expands to named rows `? icon.txt +1 -0` / `? logo.txt +1 -0` (sidebar + center, with content); no blank placeholder. Do NOT re-file. Orig: Flagship 0.4.0 Review Diff. An untracked DIRECTORY renders as `▾ dir/  +0 -0` + a file row with a **completely blank name** (`   ?   +0 -0`, verified byte-for-byte); center shows `(untracked directory)` placeholder, no hunks, `+0/-0` — contained files never listed/reviewable. Contrast: untracked FILE in a tracked dir (`new_feature.py`) shows `+2/-0` + content correctly. Mirrors `git status --short` (collapses `?? dir/`) instead of expanding like VS Code (`-uall`). Contradicts docs ("everything ... untracked in the working tree"). Workaround: `git add` the dir first. Do NOT re-file. |
| [#2314](https://github.com/sinelaw/fresh/issues/2314) | Configurable indentation rules (`[languages.<id>.indent]`) have no effect — all custom patterns ignored | Run #34 | **FIXED** (Run #46) | Closed by maintainer (0.4.2). CONFIRMED FIXED (core) in v0.4.3 via UI: custom `increase_indent_pattern:"OPEN\s*$"` now indents the next line 4sp (control w/o OPEN → col 0); Run #34's non-built-in-token no-op is resolved. NUANCE (not filed, murky→test_plan): `decrease_indent_pattern` self-dedent behavior unclear — characterize next run. Do NOT re-file. Orig: 0.4.0 headline feature broken. Custom-language `indent` block parses (language name shows in status bar) but NONE of the 5 patterns fire: `increase_indent_pattern` (end- & start-anchored), `decrease_indent_pattern`, `indent_next_line_pattern`, `dedent_next_line_pattern` all no-op when tested with non-bracket/non-colon tokens (OPEN/CLOSE/HDR/RET) that don't overlap built-in heuristics. Built-in colon/brace indent STILL fires regardless → custom block not consulted. Doc examples (`{`-end / `:`-end) coincidentally match built-in, masking it. Confirmed at both project `.fresh/config.json` and user `~/.config/fresh/config.json`. `self_close_pattern` unobservable (only modifies increase, which never fires). Do NOT re-file. |

| # | Title | Filed | Status | Notes for next run |
|---|-------|-------|--------|-------------------|
| [#2109](https://github.com/sinelaw/fresh/issues/2109) | Ctrl+H doesn't open Find & Replace in terminals (Ctrl+H = Backspace) | Run #1 | **Open** | Terminal sends `0x08`. Verify whether Calibrate Keyboard wizard detects it. Do NOT re-file. |
| [#2111](https://github.com/sinelaw/fresh/issues/2111) | Search: F3 does not navigate to next match while search bar is open | Run #1 | **Open** | Confirmed usability bug: F3 silently ignored while search bar is open. Contradicts VS Code/Sublime/browser behavior. Issue updated with clear expected vs actual. Do NOT re-file. |
| [#2112](https://github.com/sinelaw/fresh/issues/2112) | Search/Replace panel: "No matches found" for files opened outside project workspace | Run #2 | **FIXED** (Run #14) | Fixed by commit b7e7e64. Confirmed fixed in Run #14: /tmp files now appear in Search/Replace panel results. Comment added. |
| [#2113](https://github.com/sinelaw/fresh/issues/2113) | Command palette: keystrokes typed in fuzzy file mode can leak into editor buffer | Run #2 | **CLOSED not_planned** (Run #22) | Closed by maintainer 2026-06-03. Monitoring retired. Do NOT re-file. |
| [#2117](https://github.com/sinelaw/fresh/issues/2117) | Review Diff: "Discard hunk" fails with "patch does not apply" even when patch is valid | Run #5 | **FIXED** (Run #16) | Closed by maintainer. Confirmed fixed in 0.3.10 (Run #16): review_diff_test16.txt +4 lines → discard → "Review Diff: 0 hunks". File reverted to original. Do NOT re-file. |
| [#2125](https://github.com/sinelaw/fresh/issues/2125) | Diagnostics panel keyboard shortcuts (q: close, a: toggle filter, RET: goto) do not work | Run #9 | **CLOSED** (Run #16) | Closed by maintainer. Diagnostics panel 'q' confirmed still fixed in 0.3.10. *Keyboard Shortcuts* 'q' still broken → filed new #2165. Do NOT re-file. |
| [#2135](https://github.com/sinelaw/fresh/issues/2135) | Edit menu "Replace..." label maps to Ctrl+Alt+R (Query Replace), not basic Replace (Ctrl+R) | Run #13 | **FIXED** (Run #59) | CONFIRMED FIXED in v0.4.4 via UI (maintainer closed 2026-07-19; fix commit `38d6d6fac`): Edit menu now shows TWO items — `Replace... Ctrl+R` (basic; Confirm-each OFF) and `Query Replace... Ctrl+Alt+R` (interactive; Confirm-each ON), functionally proven. Do NOT re-file. |
| [#2165](https://github.com/sinelaw/fresh/issues/2165) | *Keyboard Shortcuts* buffer: pressing 'q' shows 'Editing disabled' | Run #16 | **FIXED** (Run #22) | Closed by maintainer 2026-06-07. CONFIRMED FIXED in v0.3.12 via UI ("Tab closed"). Comment added. Do NOT re-file. |
| [#2197](https://github.com/sinelaw/fresh/issues/2197) | Pyright LSP: all request-based features (hover, definition, completions) timeout after 30s | Run #17 | **Open** | Real pyright on small Python project. Initialize succeeds, all requests timeout. Position encoding mismatch suspected. **Run #59b: STILL BROKEN on v0.4.4** (hover/definition/diagnostic 30s timeout, `position_encoding=None`, pull-diagnostics also stall) — dated confirmation comment posted (comment 5019301875). First pyright recheck on the 0.4.x line. Do NOT re-file. |
| [#2212](https://github.com/sinelaw/fresh/issues/2212) | Alt+. shows "No code actions available" for diagnostic-based fixes even when clangd reports "(fix available)" | Run #19 | **FIXED** (Run #22) | Closed by maintainer 2026-06-08. CONFIRMED FIXED in v0.3.12 via UI: fix popup appears and applies. Comment added. Do NOT re-file. |
| [#2221](https://github.com/sinelaw/fresh/issues/2221) | SSH URL-style URI (`ssh://host/path`) treated as local file path instead of triggering SSH connection | Run #21 | **Open** | STILL BROKEN in v0.3.12 even with working sshd (Run #22 comment). scp-style works end-to-end. Do NOT re-file. |
| [#2291](https://github.com/sinelaw/fresh/issues/2291) | Workspace Trust: "Trust folder & Allow Tooling" restarts the editor and silently discards opened file + unsaved edits (with --no-restore) | Run #22 | **FIXED** (Run #23) | CONFIRMED FIXED in master @ f4ee3630 (v0.3.12): restart path now restores unsaved buffers from hot-exit recovery (`preserved N unnamed buffer(s)` / `Restored unsaved changes ... from hot exit recovery`). Verified via UI — file survives both Trust and Block-All restarts. Prior Run #23 (08:25Z) already commented; do NOT re-comment. Awaiting maintainer close. Do NOT re-file. |
| [#2307](https://github.com/sinelaw/fresh/issues/2307) | Keybinding Editor: switching keybinding map and back hides all plugin bindings (count drops 866 → 547) until restart | Run #28 | **FIXED** (Run #37) | Closed by maintainer 2026-06-21 (fix commit `4b6e1d2f2` "preserve plugin bindings when rebuilding the resolver"). CONFIRMED FIXED in v0.4.1 via UI: clean default load = 875 / Plugin 392 / Keymap 261; after `default→emacs→default` round-trip editor still shows **875 / Plugin 392** (was 547 / 0). Robust across a 2nd round-trip (`default→macos→default`). Comment added. Do NOT re-file. |
| [#2309](https://github.com/sinelaw/fresh/issues/2309) | Read-only buffers show no `[RO]` status-bar indicator (documented but never rendered) | Run #29 | **FIXED** (Run #46) | Closed by maintainer (0.4.2). CONFIRMED FIXED in v0.4.3 via UI: `[RO]` now a persistent status-bar segment on auto-RO library files, survives cursor movement, toggles off/on with Toggle Read-Only Mode. Do NOT re-file. Orig: Found while testing the new `editor.auto_read_only` option (Run #29). Docs (editing.md:42 + 0.2.18 blog) promise `[RO]` in status bar; actual binary renders NO persistent RO indicator — only transient `Editing disabled`/`Read-only mode enabled` messages + `[BIN]` tab tag. RO *behavior* is correct; only the indicator is missing. Do NOT re-file. |
| [#2312](https://github.com/sinelaw/fresh/issues/2312) | Occurrence highlight uses a fixed near-black background that ignores the theme (invisible in high-contrast, inverted black box in light) | Run #32 | **CLOSED by maintainer but STILL REPRODUCES** (Run #46) | Occurrence highlighting (#2154) ON by default; highlight bg is fixed color 16, not theme-derived. Maintainer closed as completed (2026-06-14) but **re-verified STILL BROKEN in v0.4.3** via ON/OFF differential ANSI: light theme → 4 `48;5;16m` boxes ON / 0 OFF; high-contrast → invisible (bg 16 == editor bg 16). **Commented on the closed issue with v0.4.3 evidence** (Run #46). Do NOT re-file (comment only); watch for a re-open. |
| [#2301](https://github.com/sinelaw/fresh/issues/2301) | Go to LSP Symbol: status bar line number stays stale after jump (only column updates) until next keypress | Run #25 | **Open** | Low-sev display bug in 0.3.12. **Run #27: confirmed NOT LSP-specific** — same staleness affects "Open file from a diff" (OLD-pane Enter → HEAD version: status shows `Ln 1,Col 1` while cursor is on the real line, self-corrects on keypress, 2/2). Comment added to #2301 broadening scope to a shared status-bar-refresh path. Cursor jumps correctly in all cases; only the status readout lags one keypress. Do NOT re-file. |

---

## Closed Issues (agent-filed — do NOT re-open or re-file)

| # | Title | Filed | Why Closed |
|---|-------|-------|-----------|
| [#2108](https://github.com/sinelaw/fresh/issues/2108) | Revert command fails when buffer has unsaved modifications | Run #1 | **False positive.** We triggered "Reload with Encoding..." not "Revert". `File > Revert` works correctly — shows `(r)evert/(c)ancel` prompt. |
| [#2110](https://github.com/sinelaw/fresh/issues/2110) | File opens as modified after session restore | Run #1 | **By design.** This is hot exit (`hot_exit` config, default on). Documented in `docs/features/session-persistence.md`. |

---

## Topics Already Investigated — Do Not Re-file

Even if the symptom looks fresh, these have already been fully investigated:

| Symptom | Conclusion | Issue |
|---------|------------|-------|
| `File > Revert` shows "Cannot reload" error | Wrong menu — that's "Reload with Encoding..." | #2108 closed |
| File opens with `[+]` / `*` on fresh launch | Hot exit restoring previous session | #2110 closed |
| `Ctrl+H` deletes a word | Terminal compat: `0x08` = Backspace | #2109 open |
| F3 does nothing during active search | F3 silently ignored while search bar is open; Enter closes bar first, then F3 works | #2111 open |
| Search/Replace panel returns "No matches found" for /tmp file | FIXED in b7e7e64. /tmp files now found via explicit queuing of out-of-root buffers. | #2112 fixed |
| Characters from command palette file search appear in editor buffer | Focus race condition during `>` → file mode transition | #2113 open |
| Review Diff `d` discard shows "Patch failed: error: patch failed" | Fresh's internal patch application is broken; manual `git apply --reverse` works | #2117 open |
| Pressing `q` in `*Keyboard Shortcuts*` buffer shows "Editing disabled" | Same root cause as #2125 — `[RO]` special buffers don't handle single-key shortcuts | #2125 open (comment added) |
| Edit menu "Replace..." shortcut is Ctrl+Alt+R (Query Replace), not Ctrl+R | Label mismatch — filed as new issue | #2135 open |

---

## How to Use This File Before Filing

1. Describe the symptom you observed in one sentence.
2. Scan the "Topics Already Investigated" table above for a match.
3. Scan the open issues table — if your topic is there, add a comment to the existing issue rather than opening a new one.
4. Search GitHub with at least 3 different query variations.
5. Only then open a new issue and add a row to this file.

## Issue #2312 — Occurrence highlight uses a fixed near-black background that ignores the theme
- **Filed:** Run #32, 2026-06-10
- **URL:** https://github.com/sinelaw/fresh/issues/2312
- **Label:** bug, tui-agent-auto-bug
- **Status:** Open
- **Summary:** Occurrence highlighting (PR #2154, ON by default) draws its highlight using a fixed 256-color index **16** (near-black) that does NOT derive from the active theme. **high-contrast theme:** editor bg is also color 16, so the highlight is invisible — proven by an ON/OFF differential ANSI capture where toggling Occurrence Highlight changes nothing on any non-current line; the only visible effect is the current-line word drawn bg 16 (DARKER than the current-line bg 233 → recessed, not highlighted). **light theme:** occurrences render as inverted solid black boxes (bg 16 on a ~231/254 background). **dark/dracula/nostalgia:** bg 16 on ~234/235 is a subtle box and looks fine (why it went unnoticed). Reference: VS Code uses theme-defined `editor.wordHighlightBackground`/`wordHighlightStrongBackground` so the cue is always visible and theme-appropriate. The *whole-word* matching and toggle behavior are correct — only the color is hard-wired. Reproduces with default config (just switch theme via palette); occurrence highlighting needs no config.
- **Search queries used (no dup):** `occurrence highlight theme color`, `occurrence highlight high-contrast invisible`, `word highlight background color not theme aware`, `highlight_occurrences`

## Issue #2309 — Read-only buffers show no `[RO]` status-bar indicator
- **Filed:** Run #29, 2026-06-10
- **URL:** https://github.com/sinelaw/fresh/issues/2309
- **Label:** bug, tui-agent-auto-bug
- **Status:** Open
- **Summary:** Discovered while testing the brand-new `editor.auto_read_only` config option (commit 9738ac661). Fresh's docs (`docs/features/editing.md:42` and `docs/blog/fresh-0.2.18`) both state read-only buffers show `[RO]` in the status bar. In v0.3.12 (built from origin/master @ 2dee83697) NO persistent `[RO]` indicator is rendered for ANY read-only buffer — library-path auto-RO (`/usr/include/stdio.h`), binary files, or manual Toggle Read-Only Mode. `grep RO` over the full captured screen = 0 matches while a buffer is read-only. The only feedback is transient one-shot status messages (`Editing disabled in this buffer`, `Read-only mode enabled/disabled`) and the `[BIN]` tab tag (which means "binary", not "read-only"). The read-only *behavior* itself is correct (auto-RO triggers, `auto_read_only:false` disables it, binaries stay RO regardless, Toggle works) — only the documented indicator is absent. Usability gap: a user has no standing cue a buffer is read-only until an edit silently fails.
- **Search queries used:** `read-only indicator status bar RO`, `[RO] read only buffer indicator`, `read-only mode no visual indication editing disabled` (all 0 results)

## Issue #2307 — Keybinding Editor hides all plugin bindings after keymap round-trip
- **Filed:** Run #28, 2026-06-10
- **URL:** https://github.com/sinelaw/fresh/issues/2307
- **Label:** bug, tui-agent-auto-bug
- **Status:** Open
- **Summary:** Resolves the long-standing "866 vs 548" Keybinding Editor count anomaly first noted in Run #22 (test_plan priority #8). On a clean `default` keymap the editor lists 866 bindings (Source[Plugin]=391/866, Source[Keymap]=260/866). A SINGLE "Select Keybinding Map" round-trip — `default → emacs → default` — drops the editor to 547 bindings with Source[Plugin]=0/547 (all 391 plugin bindings vanish); Keymap stays 260. 100% reproducible, persists across editor reopens + multi-second wait. Per-map first-load totals are each stable/correct (default 866, emacs 519, macos 600) — bug is only on *return* to an already-loaded map. **Plugin bindings still FUNCTION** (Alt+O = Toggle Orchestrator Dock Focus still opens dock after round-trip), so it is a listing/reporting defect, not loss of functionality. Workaround: restart Fresh.
- **Search queries used:** `keybinding editor binding count`, `Select Keybinding Map plugin bindings disappear`, `keybinding editor 866 547 count drops`, `active_keybinding_map switch plugin` (no duplicate)

## Issue #2291 — Workspace Trust restart discards opened file + unsaved edits
- **Filed:** Run #22, 2026-06-09
- **URL:** https://github.com/sinelaw/fresh/issues/2291
- **Label:** bug, tui-agent-auto-bug
- **Status:** Open
- **Summary:** Confirming "Trust folder & Allow Tooling" in the Workspace Trust dialog performs a full editor restart (`Restart requested with new working directory` in log, even when cwd is unchanged). With `--no-restore`, the CLI-opened file and any unsaved edits are silently discarded — no save prompt, no recovery offer (a recovery chunk IS written under `~/.local/share/fresh/recovery/` but never surfaced). In default mode session restore rebuilds the buffers (incl. unsaved edits), so only the File Explorer auto-open quirk is visible. "Keep Restricted" does not restart. Reference: VS Code keeps open editors and unsaved content when trusting a workspace.
- **Search queries used:** `workspace trust restart` (only #2280, different), `trust folder file closed unsaved`, `security warning dialog buffer lost`, `trust prompt no-restore data loss`, `"Trust folder" OR "Allow Tooling" editor restart`

## Issue #2221 — SSH URL-style URI (`ssh://host/path`) treated as local file path
- **Filed:** Run #21, 2026-06-03
- **URL:** https://github.com/sinelaw/fresh/issues/2221
- **Label:** bug, tui-agent-auto-bug
- **Status:** Open
- **Summary:** When Fresh is launched with `ssh://host/path`, the URI is resolved as a local relative path (CWD + URI string), resulting in an empty local file being opened silently. No SSH connection is attempted, no error is shown. The scp-style form (`user@host:/path`) correctly triggers the SSH code path. Log evidence: `path="/home/user/fresh/ssh://localhost/etc/hosts"`. The `ssh://` form is documented in `docs/features/ssh.md` as the preferred URL-style form.
- **Search queries used:** `repo:sinelaw/fresh ssh:// URI not recognized`, `repo:sinelaw/fresh ssh URL local path remote`, `repo:sinelaw/fresh ssh:// treated as local file path`, `repo:sinelaw/fresh ssh remote editing URL form`, `repo:sinelaw/fresh "ssh://" command line argument`

## Issue #2212 — Alt+. shows "No code actions available" for diagnostic-based fixes
- **Filed:** Run #19, 2026-06-03
- **URL:** https://github.com/sinelaw/fresh/issues/2212
- **Label:** bug, tui-agent-auto-bug
- **Status:** Open
- **Summary:** Fresh always sends `"context":{"diagnostics":[]}` (empty) in every `textDocument/codeAction` request. When clangd publishes diagnostics with "(fix available)" markers, pressing Alt+. shows "No code actions available" instead of the fix popup. Root cause confirmed via LSP log: clangd's `publishDiagnostics` was received correctly, but codeAction request omits them. This is the "TODO: Implement diagnostic retrieval when needed" left from closed issue #1915 — the capability fix in v0.3.6 resolved rust-analyzer WorkspaceEdit actions but did not implement `context.diagnostics` population.
- **Search queries used:** `code actions diagnostics empty no code actions available`, `LSP code action context diagnostics clangd`, `"no code actions available" clangd`, `code action fix available diagnostic context`

## Issue #2165 — *Keyboard Shortcuts* buffer 'q' does not close (persists in 0.3.10)
- **Filed:** Run #16, 2026-05-31
- **URL:** https://github.com/sinelaw/fresh/issues/2165
- **Label:** bug, tui-agent-auto-bug
- **Status:** Open
- **Summary:** The `*Keyboard Shortcuts*` buffer opened via `Shift+F1` documents "Press 'q' to close this buffer." on line 4, but pressing 'q' shows "Editing disabled in this buffer" in 0.3.10. All other special buffers (Diagnostics, Git Blame, Dev Container) correctly handle 'q'. The Diagnostics panel fix in commit 89caf72 used `panelKeys` for that plugin — this buffer uses a different mechanism that was not updated. Parent issue #2125 closed without fixing this.
- **Search queries used:** `keyboard shortcuts q editing disabled`, `keyboard shortcuts buffer close`, `Shift+F1 q close`

## Issue #2122 — move_to_paragraph_down/up Has No Default Keybinding (0.3.9 oversight)
- **Filed:** Run #7, 2026-05-26
- **URL:** https://github.com/sinelaw/fresh/issues/2122
- **Label:** bug, tui-agent-auto-bug
- **Status:** Open
- **Summary:** The `move_to_paragraph_down` and `move_to_paragraph_up` actions added in 0.3.9 (PR #2084) have no default keybinding and are not accessible from the command palette. The sibling `select_to_paragraph_*` actions have `Ctrl+Shift+↓/↑` bindings. Users cannot use the new "jump to paragraph" feature without manually binding it.
- **Search queries used:** `move_to_paragraph keybinding`, `paragraph navigation keybinding`, `paragraph action command palette missing`
